# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: snowpt
  name: SnowPT
  description: SnowPT (SSPT) is a CHINESE Private Torrent Tracker for ANIME
  language: zh-CN
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/snowpt.yml
    sourceId: snowpt
    license: GPL-2.0-only
    fileSha256: f6661577fd103f004d6058f9169c77e263a82d09295463e1db046eeedf554681
    contentSha256: 0a0f12665047fb879ecb03293e2a8535f8141a201ffa47c91a995041088e0681
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://skyeysnow.com/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 1
        cat: TV/Anime
        desc: Anime
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
    settings:
    - name: username
      type: text
      label: Username
    - name: password
      type: password
      label: Password
    - name: freeleech
      type: checkbox
      label: Filter freeleech only
      default: false
    authentication:
      path: login.php
      method: form
      form: form[id^="loginform_"]
      inputs:
        referer: /
        loginfield: username
        username: '{{ .Config.username }}'
        password: '{{ .Config.password }}'
        cookietime: 2592000
      error:
      - selector: div#messagetext:contains("登录失败") p
      test:
        path: /
        selector: a[href*="action=logout&formhash="]
    search:
      paths:
      - path: forum.php
      inputs:
        mod: torrents
        cat: 1
        search: '{{ .Keywords }}'
      rows:
        selector: table.torrents > tbody > tr:has(a[href^="/download.php?id="]){{ if .Config.freeleech }}:has(img.sp_4){{
          else }}{{ end }}
      fields:
        category:
          text: 1
        title_default:
          selector: a[href^="/forum.php?mod=viewthread&tid="]
        title:
          selector: a[title][href^="/forum.php?mod=viewthread&tid="]
          attribute: title
          optional: true
          default: '{{ .Result.title_default }}'
        details:
          selector: a[href^="/forum.php?mod=viewthread&tid="]
          attribute: href
        download:
          selector: a[href^="/download.php?id="]
          attribute: href
        date_elapsed:
          selector: td:nth-child(3) > span[title]
          attribute: title
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-dd HH:mm:ss zzz
        date_added:
          selector: td:nth-child(3):not(:has(span))
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-ddHH:mm:ss zzz
        date:
          text: '{{ if or .Result.date_elapsed .Result.date_added }}{{ or .Result.date_elapsed .Result.date_added }}{{ else
            }}now{{ end }}'
        size:
          selector: td:nth-child(4)
        seeders:
          selector: td:nth-child(5)
        leechers:
          selector: td:nth-child(6)
        grabs:
          selector: td:nth-child(7)
        downloadvolumefactor:
          case:
            img.sp_4: 0
            img.sp_2: 0.5
            '*': 1
        uploadvolumefactor:
          case:
            '*': 1

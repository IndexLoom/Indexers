# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: byrbt
  name: BYRBT
  description: BYRBT is a CHINESE Private Torrent Tracker for HD MOVIES / TV / GENERAL
  language: zh-CN
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/byrbt.yml
    sourceId: byrbt
    license: GPL-2.0-only
    fileSha256: 434b49c5755966d313ecff852f888040b3bb25a01baa65db76bf4af7ec3c5161
    contentSha256: 9f3fb7756bf622a64e57e18658809b36c652990eae68d8d3d0d58cfff773b2bb
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://byr.pt/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 408
        cat: Movies
        desc: 电影Movies
      - id: 401
        cat: TV
        desc: 剧集TV Series
      - id: 404
        cat: TV/Anime
        desc: 动漫Animations
      - id: 402
        cat: Audio
        desc: 音乐Music
      - id: 405
        cat: Other
        desc: 综艺Arts
      - id: 403
        cat: Console
        desc: 游戏Games
      - id: 406
        cat: PC
        desc: 软件Software
      - id: 407
        cat: TV/Documentary
        desc: 资料Documentaries
      - id: 409
        cat: TV/Sport
        desc: 体育Sports
      - id: 410
        cat: Other/Misc
        desc: 纪录Records
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        - doubanid
        movie-search:
        - q
        - imdbid
        - doubanid
        music-search:
        - q
    settings:
    - name: cookie
      type: text
      label: Cookie
    - name: info_cookie
      type: info_cookie
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: 4
      options:
        4: created
        7: seeders
        5: size
        1: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_tpp
      type: info
      label: Results Per Page
      default: For best results, change the <b>Torrents per page:</b> setting to <b>100</b> on your account profile.
    - name: info_ipv6
      type: info
      label: IPv6 only
      default: This site only accepts IPv6 addresses. If you are using IPv4 you will get a DNS Lookup Error.
    - name: info_activity
      type: info
      label: Account Inactivity
      default: Account retention rules:<ol><li>Newly registered users who have not generated any traffic within 14 days (that
        is, the upload/download data are both 0) will be deleted</li><li>Users with a user level of Peasant that have failed
        to successfully improve their sharing rate within 14 days will be deleted.</li></ol>
    authentication:
      method: cookie
      inputs:
        cookie: '{{ .Config.cookie }}'
      test:
        path: index.php
        selector: a[href^="logout.php?key="]
    search:
      paths:
      - path: torrents.php
      inputs:
        $raw: '{{ range .Categories }}cat{{.}}=1&{{end}}'
        search: '{{ if .Query.IMDBID }}{{ .Query.IMDBID }}{{ else }}{{ end }}{{ if and .Query.DoubanID (eq .Query.IMDBID .False)
          }}{{ .Query.DoubanID }}{{ else }}{{ end }}{{ if and (eq .Query.IMDBID .False) (eq .Query.DoubanID .False) }}{{ .Keywords
          }}{{ else }}{{ end }}'
        incldead: 0
        spstate: '{{ if .Config.freeleech }}2{{ else }}0{{ end }}'
        search_area: '{{ if .Query.IMDBID }}4{{ else }}{{ end }}{{ if and .Query.DoubanID (eq .Query.IMDBID .False) }}1{{
          else }}{{ end }}{{ if and (eq .Query.IMDBID .False) (eq .Query.DoubanID .False) }}0{{ else }}{{ end }}'
        search_mode: 0
        sort: '{{ .Config.sort }}'
        type: '{{ .Config.type }}'
        notnewword: 1
      rows:
        selector: table.torrents > tbody > tr:has(table.torrentname)
      fields:
        category:
          selector: a[href^="torrents.php?cat="]
          attribute: href
          filters:
          - name: querystring
            args: cat
        title_default:
          selector: a[href^="details.php?id="]
        title:
          selector: a[title][href^="details.php?id="]
          attribute: title
          optional: true
          default: '{{ .Result.title_default }}'
        details:
          selector: a[href^="details.php?id="]
          attribute: href
        download:
          selector: a[href^="details.php?id="]
          attribute: href
          filters:
          - name: replace
            args:
            - details
            - download
        imdbid:
          selector: a[href*="imdb.com/title/tt"]
          attribute: href
        date_elapsed:
          selector: td.rowfollow:nth-last-of-type(6) > span[title]
          attribute: title
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-dd HH:mm:ss zzz
        date_added:
          selector: td.rowfollow:nth-last-of-type(6):not(:has(span))
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-ddHH:mm:ss zzz
        date:
          text: '{{ if or .Result.date_elapsed .Result.date_added }}{{ or .Result.date_elapsed .Result.date_added }}{{ else
            }}now{{ end }}'
        size:
          selector: td.rowfollow:nth-last-of-type(5)
        seeders:
          selector: td.rowfollow:nth-last-of-type(4)
        leechers:
          selector: td.rowfollow:nth-last-of-type(3)
        grabs:
          selector: td.rowfollow:nth-last-of-type(2)
        downloadvolumefactor:
          case:
            img.pro_free: 0
            img.pro_free2up: 0
            img.pro_50pctdown: 0.5
            img.pro_50pctdown2up: 0.5
            img.pro_30pctdown: 0.3
            '*': 1
        uploadvolumefactor:
          case:
            img.pro_50pctdown2up: 2
            img.pro_free2up: 2
            img.pro_2up: 2
            '*': 1
        minimumratio:
          text: 1.0
        minimumseedtime:
          text: 86400
        description:
          selector: td:nth-child(2)
          remove: a, b, font, img, span

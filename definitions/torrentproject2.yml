# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: torrentproject2
  name: TorrentProject2
  description: TorrentProject2 is a Public torrent meta-search engine
  language: en-US
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/torrentproject2.yml
    sourceId: torrentproject2
    license: GPL-2.0-only
    fileSha256: e470f4a602578c92a6371f81fbd1c01dff10d28a0bc899632d0c399f7a64e811
    contentSha256: d7af51262baadf7c68f3d85932fabb08f94ce87307a85e4ab91afba201c942dc
spec:
  protocol: torrent
  privacy: public
  baseUrls:
  - https://torrentproject2.net/
  - https://torrentproject2.org/
  - https://torrentproject.info/
  - https://torrentproject.biz/
  - https://torrentproject.xyz/
  - https://torrentproject.cc/
  legacyUrls:
  - https://torrentproject2.se/
  - https://torrentproject2.com/
  - https://torrentproject.torrentbay.st/
  implementation:
    engine: cardigann
    encoding: UTF-8
    requestDelaySeconds: 2
    capabilities:
      categorymappings:
      - id: Other
        cat: Other
        desc: Other
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        movie-search:
        - q
        music-search:
        - q
        book-search:
        - q
    settings:
    - name: filter-verified
      type: checkbox
      label: Only include verifed content in results
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: latest
      options:
        latest: created desc
        oldest: created asc
        seeders: seeders
        size: size
    - name: info_category_8000
      type: info_category_8000
    download:
      selectors:
      - selector: '#download > div:nth-child(2) > div:nth-child(1) > a'
        attribute: href
        filters:
        - name: replace
          args:
          - https://mylink.me.uk/?url=
          - ''
        - name: replace
          args:
          - https://mylink.cx/?url=
          - ''
        - name: replace
          args:
          - https://mylink.cloud/?url=
          - ''
        - name: urldecode
    search:
      paths:
      - path: '{{ if .Keywords }}/{{ else }}browse{{ end }}'
      - path: '{{ if .Keywords }}/{{ else }}browse{{ end }}'
        inputs:
          p: 1
      inputs:
        t: '{{ .Keywords }}'
        orderby: '{{ if .Keywords }}{{ .Config.sort }}{{ else }}{{ end }}'
        safe: '{{ if and .Keywords .Config.filter-verified }}on{{ else }}{{ end }}'
      rows:
        selector: '#similarfiles div:has(a[href^="/t"])'
      fields:
        category:
          text: Other
        title:
          selector: :scope > span > a
        details:
          selector: :scope > span > a
          attribute: href
        download:
          selector: :scope > span > a
          attribute: href
        date_ago:
          selector: :scope > span:nth-child(4):contains("ago")
          optional: true
        date_time:
          selector: :scope > span:nth-child(4):contains(":")
          optional: true
          filters:
          - name: append
            args: ' -07:00'
          - name: dateparse
            args: yyyy-MM-dd HH:mm:ss zzz
        date:
          text: '{{ if or .Result.date_ago .Result.date_time }}{{ or .Result.date_ago .Result.date_time }}{{ else }}now{{
            end }}'
        size:
          selector: :scope > span:nth-child(5)
        seeders:
          selector: :scope > span:nth-child(2)
        leechers:
          selector: :scope > span:nth-child(3)
        downloadvolumefactor:
          text: 0
        uploadvolumefactor:
          text: 1

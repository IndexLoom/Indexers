# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: trancetraffic
  name: TranceTraffic
  description: TranceTraffic is a Private Torrent Tracker for MUSIC
  language: en-US
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/trancetraffic.yml
    sourceId: trancetraffic
    license: GPL-2.0-only
    fileSha256: 9c32c2f12a6630004d2d1070cae6b350e6f8aca21a0f7724249b07dd681c04ba
    contentSha256: cd4c2677ae5d6b03412b286d8d23248f87289b671470ab5ac60556d07c9eceef
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://www.trancetraffic.com/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categories:
        1: Audio
      modes:
        search:
        - q
        music-search:
        - q
        - artist
    settings:
    - name: username
      type: text
      label: Username
    - name: password
      type: password
      label: Password
    - name: freeleech
      type: checkbox
      label: Filter freeleech only
      default: false
    - name: info_tpp
      type: info
      label: Results Per Page
      default: For best results, change the <b>Torrents per page:</b> setting to <b>100</b> on your account profile.
    authentication:
      path: takelogin.php
      method: post
      inputs:
        username: '{{ .Config.username }}'
        password: '{{ .Config.password }}'
      error:
      - selector: td.embedded:has(h2:contains("failed"))
      test:
        path: index.php
        selector: a[href*="logout.php"]
    search:
      paths:
      - path: browse.php
      inputs:
        search: '{{ if .Query.Artist }}{{ .Query.Artist }}{{ else }}{{ .Keywords }}{{ end }}'
        includeFL: '{{ if .Config.freeleech }}on{{ else }}{{ end }}'
      rows:
        selector: table.mainouter table > tbody > tr:has(a[href^="details.php?id="])
      fields:
        category:
          text: 1
        download:
          selector: a[href^="download.php/"]
          attribute: href
        title:
          selector: a[href^="details.php?id="]
        details:
          selector: a[href^="details.php?id="]
          attribute: href
          filters:
          - name: replace
            args:
            - '&hit=1'
            - ''
        date:
          selector: td:nth-child(6)
          filters:
          - name: dateparse
            args: yyyy-MM-ddHH:mm:ss
        seeders:
          selector: td:nth-child(9)
        leechers:
          selector: td:nth-child(10)
        grabs:
          selector: td:nth-child(8)
        files:
          selector: td:nth-child(4)
        size:
          selector: td:nth-child(7)
        downloadvolumefactor:
          case:
            span:contains("FREELEECH"): 0
            '*': 1
        uploadvolumefactor:
          text: 1

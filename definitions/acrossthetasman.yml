# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: acrossthetasman
  name: Across The Tasman
  description: ATT is a Private torrent site for Rugby and other sports played in Australia
  language: en-AU
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/acrossthetasman.yml
    sourceId: acrossthetasman
    license: GPL-2.0-only
    fileSha256: 2cce25d1ad5e2b8419a42103daa03041375940bda048ae18fceb8f8e64d8d2be
    contentSha256: a39e175773921832252308a8e387e097e8f759dd02d3ef9581374543e20717a4
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://acrossthetasman.com/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categories:
        1: TV/Sport
      modes:
        search:
        - q
    settings:
    - name: username
      type: text
      label: Username
    - name: password
      type: password
      label: Password
    - name: sort
      type: select
      label: Sort requested from site
      default: date
      options:
        date: created
        filename: title
        size: size
        seeders: seeders
    - name: order
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    authentication:
      method: post
      path: login.php
      inputs:
        vb_login_username: '{{ .Config.username }}'
        vb_login_password: '{{ .Config.password }}'
        cookieuser: 1
        do: login
      error:
      - selector: div.restore:contains("invalid username or password")
      test:
        path: torrents.php
        selector: a[href*="login.php?do=logout"]
    search:
      paths:
      - path: torrents.php
      inputs:
        sort: '{{ .Config.sort }}'
        order: '{{ .Config.order }}'
        query: '{{ .Keywords }}'
      rows:
        selector: table[width='100%'][align='center']:not([class]) > tbody > tr
      fields:
        category:
          text: 1
        date:
          selector: td:nth-child(1)
          filters:
          - name: dateparse
            args: MMM dd, yyyy
        title:
          selector: td:nth-child(4) > span:nth-child(1)
          filters:
          - name: re_replace
            args:
            - '[\s]+Uploaded.*'
            - ''
        download:
          selector: a[href*="attachment.php?attachmentid="]
          attribute: href
        seeders:
          selector: td:nth-child(5)
        leechers:
          selector: td:nth-child(6)
        grabs:
          selector: td:nth-child(7)
        size:
          selector: td:nth-child(10)

# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: tjupt
  name: TJUPT (北洋园PT)
  description: TJUPT (北洋园PT) is a CHINESE Private Torrent Tracker for MOVIES / TV / GENERAL
  language: zh-CN
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/tjupt.yml
    sourceId: tjupt
    license: GPL-2.0-only
    fileSha256: 83891e855529b9a993735e5e12c45542f33fbf22075378366cc8b5de573da580
    contentSha256: fab61e765a92a5af7466396d5139255362fb2c4d83587d6cfa58e0c093919d5e
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://tjupt.org/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 401
        cat: Movies
        desc: Movies电影
      - id: 402
        cat: TV
        desc: TV Series剧集
      - id: 403
        cat: TV
        desc: TV Shows综艺
      - id: 404
        cat: TV/Documentary
        desc: Documentaries资料
      - id: 405
        cat: TV/Anime
        desc: Animations动漫
      - id: 406
        cat: Audio/Video
        desc: Music Videos音乐
      - id: 407
        cat: TV/Sport
        desc: Sports体育
      - id: 408
        cat: PC
        desc: Software软件
      - id: 409
        cat: Console
        desc: Games游戏
      - id: 411
        cat: Books
        desc: Books纪录片
      - id: 412
        cat: PC/Mobile-Other
        desc: Mobile移动视频
      - id: 410
        cat: Other
        desc: Misc其他
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        - doubanid
        movie-search:
        - q
        - imdbid
        - doubanid
        book-search:
        - q
    settings:
    - name: cookie
      type: text
      label: Cookie
    - name: info_cookie
      type: info_cookie
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: 4
      options:
        4: created
        7: seeders
        5: size
        1: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_tpp
      type: info
      label: Results Per Page
      default: For best results, change the <b>Torrents per page:</b> setting to <b>100</b> on your account profile.
    - name: info_activity
      type: info
      label: Account Inactivity
      default: 'Banning rules for account inactivity:<ol><li>Within 180 days, an account is considered active if it meets
        any of the following conditions: has checked in for at least 1 day; has published at least 1 seed, fun box or new
        forum topic (non-reply); actually uploaded increments > 5TiB and the seeding integral increment is > 30W</li><li>Users
        of Megatron level and above ignore the inactivity rules and will keep their accounts forever</li><li>Users with Sword
        Down the Mountain and above will not be disabled due to inactivity after parking their accounts in the control panel</li><li>Un-parked
        accounts below the level of Megatron will be disabled if they have been ''inactive'' for 180 consecutive days (Effective
        from 2024).</li><li>Accounts parked below the level of Down the Mountain with a Sword will be disabled if they are
        ''inactive'' for 300 consecutive days (Effective from 2024).</li></ol>'
    authentication:
      method: cookie
      inputs:
        cookie: '{{ .Config.cookie }}'
      test:
        path: index.php
        selector: a[href="logout.php"]
    search:
      paths:
      - path: torrents.php
      inputs:
        $raw: '{{ range .Categories }}cat{{.}}=1&{{end}}'
        search: '{{ if .Query.IMDBID }}{{ .Query.IMDBID }}{{ else }}{{ end }}{{ if and .Query.DoubanID (eq .Query.IMDBID .False)
          }}{{ .Query.DoubanID }}{{ else }}{{ end }}{{ if and (eq .Query.IMDBID .False) (eq .Query.DoubanID .False) }}{{ .Keywords
          }}{{ else }}{{ end }}'
        incldead: 0
        picktype: 0
        spstate: '{{ if .Config.freeleech }}9{{ else }}0{{ end }}'
        search_area: '{{ if .Query.IMDBID }}4{{ else }}{{ end }}{{ if and .Query.DoubanID (eq .Query.IMDBID .False) }}1{{
          else }}{{ end }}{{ if and (eq .Query.IMDBID .False) (eq .Query.DoubanID .False) }}0{{ else }}{{ end }}'
        search_mode: 0
        sort: '{{ .Config.sort }}'
        type: '{{ .Config.type }}'
        notnewword: 1
      rows:
        selector: table.torrents > tbody > tr:has(table.torrentname)
      fields:
        category:
          selector: a[href*="cat="]
          attribute: href
          filters:
          - name: querystring
            args: cat
        title_default:
          selector: a[href^="details.php?id="]
        title:
          selector: a[title][href^="details.php?id="]
          attribute: title
          optional: true
          default: '{{ .Result.title_default }}'
          filters:
          - name: re_replace
            args:
            - ^\[([^\]]+)\]\[([^\]]+)\]\[([^\]]+)\](.*)$
            - $3 [$1][$2]$4
        details:
          selector: a[href^="details.php?id="]
          attribute: href
        download:
          selector: a[href^="details.php?id="]
          attribute: href
          filters:
          - name: replace
            args:
            - details
            - download
        imdbid:
          selector: a[href*="imdb.com/title/tt"]
          attribute: href
        date_elapsed:
          selector: td.rowfollow:nth-last-child(6) > span[title]
          attribute: title
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-dd HH:mm:ss zzz
        date_added:
          selector: td.rowfollow:nth-last-child(6):not(:has(span))
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-ddHH:mm:ss zzz
        date:
          text: '{{ if or .Result.date_elapsed .Result.date_added }}{{ or .Result.date_elapsed .Result.date_added }}{{ else
            }}now{{ end }}'
        size:
          selector: td.rowfollow:nth-last-child(5)
        seeders:
          selector: td.rowfollow:nth-last-child(4)
        leechers:
          selector: td.rowfollow:nth-last-child(3)
        grabs:
          selector: td.rowfollow:nth-last-child(2)
        downloadvolumefactor:
          case:
            img.pro_free: 0
            img.pro_free2up: 0
            img.pro_50pctdown: 0.5
            img.pro_50pctdown2up: 0.5
            img.pro_30pctdown: 0.3
            '*': 1
        uploadvolumefactor_custom:
          selector: span:has(img.arrowup)
          optional: true
          filters:
          - name: replace
            args:
            - X
            - ''
        uploadvolumefactor:
          case:
            img.pro_50pctdown2up: 2
            img.pro_free2up: 2
            img.pro_2up: 2
            span:has(img.arrowup): '{{ .Result.uploadvolumefactor_custom }}'
            '*': 1
        minimumratio:
          text: 0.8
        description:
          selector: td:nth-child(2)
          remove: a, b, font, img, span

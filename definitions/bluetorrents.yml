# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: bluetorrents
  name: BlueTorrents
  description: BlueTorrents is a Private Torrent Tracker for MOVIES / TV / MUSIC / GENERAL
  language: en-US
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/bluetorrents.yml
    sourceId: bluetorrents
    license: GPL-2.0-only
    fileSha256: 782d8a71f5efcfab487dab16623d37454b6e09eeca4a72cae499e9fb87a8a76e
    contentSha256: d8fe5a99cfe2b969a147ed167c5584e2b3ef4d09940be5ee72e4a4c173700c18
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://bluetorrents.com/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 2000
        cat: Movies
        desc: Movies
      - id: 5000
        cat: TV
        desc: TV Shows
      - id: 1000
        cat: Console
        desc: Games
      - id: 3000
        cat: Audio
        desc: Music
      - id: 4000
        cat: PC/0day
        desc: Apps
      - id: 6000
        cat: XXX
        desc: XXX
      - id: 8000
        cat: Other
        desc: Other
      - id: 7000
        cat: Books
        desc: Books
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        movie-search:
        - q
        - tmdbid
        music-search:
        - q
        book-search:
        - q
      allowtvsearchimdb: true
    settings:
    - name: apikey
      type: text
      label: API Key
    - name: info_key
      type: info
      label: About your API key
      default: You can find your API Key by accessing the <a href="https://bluetorrents.com/settings" target="_blank">BlueTorrents
        Settings</a> page, and scrolling down to the <b>API keys</b> section.
    - name: info_3x
      type: info
      label: Including 3X
      default: IF you want to see 3x torrents access your <a href="https://bluetorrents.com/settings" target="_blank">BlueTorrents
        Settings</a> page and tick the <b>Show NSFW content</b> checkbox.
    - name: info_activity
      type: info
      label: Account Inactivity
      default: Accounts with no activity for 90+ days may be disabled.
    authentication:
      path: api/external
      method: get
      inputs:
        t: search
        apikey: '{{ .Config.apikey }}'
        limit: 1
      error:
      - selector: error
        message:
          selector: error[description]
          attribute: description
    search:
      paths:
      - path: api/external
        response:
          type: xml
      inputs:
        apikey: '{{ .Config.apikey }}'
        t: '{{ if .Query.TMDBID }}movie{{ else }}{{ end }}{{ if or .Query.Season .Query.Ep }}tvsearch{{ else }}{{ end }}{{
          if or .Query.IMDBID .Query.TMDBID .Query.Season .Query.Ep }}{{ else }}search{{ end }}'
        q: '{{ .Keywords }}'
        cat: '{{ join .Categories "," }}'
        season: '{{ .Query.Season }}'
        ep: '{{ .Query.Ep }}'
        imdbid: '{{ .Query.IMDBID }}'
        tmdbid: '{{ .Query.TMDBID }}'
        limit: 100
      keywordsfilters:
      - name: re_replace
        args:
        - \W+
        - '%'
      rows:
        selector: rss > channel > item
      fields:
        category:
          selector: '[name=category]'
          attribute: value
        title:
          selector: title
        details:
          selector: link
        download:
          selector: enclosure
          attribute: url
        infohash:
          selector: '[name=infohash]'
          attribute: value
        imdbid:
          selector: '[name=imdbid]'
          attribute: value
        tmdbid:
          selector: '[name=tmdbid]'
          attribute: value
        date:
          selector: pubDate
          filters:
          - name: dateparse
            args: ddd, dd MMM yyyy HH:mm:ss zzz
        size:
          selector: size
        seeders:
          selector: '[name=seeders]'
          attribute: value
        leechers:
          selector: '[name=peers]'
          attribute: value
        grabs:
          selector: '[name=grabs]'
          attribute: value
        downloadvolumefactor:
          selector: '[name=downloadvolumefactor]'
          attribute: value
        uploadvolumefactor:
          selector: '[name=uploadvolumefactor]'
          attribute: value
        minimumratio:
          text: 1.0
        minimumseedtime:
          text: 259200

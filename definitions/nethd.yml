# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: nethd
  name: NetHD
  description: NetHD (VietTorrent) is a VIETNAMESE Semi-Private Torrent Tracker for HD MOVIES / TV
  language: vi-VN
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/nethd.yml
    sourceId: nethd
    license: GPL-2.0-only
    fileSha256: c7c2925041892834dfbc33ecb02a0a80e4d9742eed64a7452fadb38523b1dbb3
    contentSha256: 7f1ebcc0a4d316508741d25d8a7945cf95305ec11b41e908855a2654d8a0cf4e
spec:
  protocol: torrent
  privacy: semi-private
  baseUrls:
  - https://nethd.org/
  legacyUrls:
  - http://nethd.org/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 401
        cat: Movies
        desc: Movie
      - id: 401
        cat: TV
        desc: TV
      - id: 402
        cat: Audio
        desc: Music
      - id: 403
        cat: PC/Games
        desc: Game
      - id: 404
        cat: PC
        desc: Software
      - id: 405
        cat: Other
        desc: Image
      - id: 406
        cat: Books
        desc: Book
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        movie-search:
        - q
        - imdbid
        music-search:
        - q
        book-search:
        - q
    settings:
    - name: username
      type: text
      label: Username
    - name: password
      type: password
      label: Password
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: added
      options:
        added: created
        seeders: seeders
        size: size
        name: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_tpp
      type: info
      label: Results Per Page
      default: For best results, change the <b>Torrents per page:</b> setting to <b>100</b> on your account profile.
    - name: info_activity
      type: info
      label: Account Inactivity
      default: If the account is not used regularly within 2 years, the system will automatically delete the account.
    authentication:
      path: takelogin.php
      method: post
      inputs:
        username: '{{ .Config.username }}'
        password: '{{ .Config.password }}'
        logout: ''
        securelogin: ''
        ssl: 'yes'
        trackerssl: 'yes'
      error:
      - selector: form#loginform > span.warning
      test:
        path: index.php
        selector: a[href="logout.php"]
    search:
      paths:
      - path: torrents.php
      inputs:
        $raw: '{{ range .Categories }}cat={{.}}&{{end}}'
        search: '{{ if .Query.IMDBID }}{{ .Query.IMDBID }}{{ else }}{{ .Keywords }}{{ end }}'
        search_area: '{{ if .Query.IMDBID }}4{{ else }}0{{ end }}'
        search_mode: 0
        spstate: '{{ if .Config.freeleech }}2{{ else }}0{{ end }}'
        incldead: 0
        sort: '{{ .Config.sort }}'
        type: '{{ .Config.type }}'
        notnewword: 1
      rows:
        selector: tr:has(td.name)
        filters:
        - name: andmatch
      fields:
        title:
          selector: td.name > div > a[href*="-torrent-"][title]
          attribute: title
        category_is_tv_show:
          text: '{{ .Result.title }}'
          filters:
          - name: regexp
            args: \b(S\d+(?:E\d+)?)\b
        categorydesc:
          selector: td.category > a
          attribute: title
          filters:
          - name: replace
            args:
            - movie
            - '{{ if .Result.category_is_tv_show }}tv{{ else }}movie{{ end }}'
        details:
          selector: td.name > div > a[href*="-torrent-"][title]
          attribute: href
        poster:
          selector: td.name > a.poster-preview img
          attribute: src
        download:
          selector: td.name > div > a[href*="-torrent-"][title]
          attribute: href
          filters:
          - name: regexp
            args: -torrent-(\d+).html
          - name: prepend
            args: download.php?id=
        date_year:
          selector: td:nth-child(5):contains("-")
          optional: true
          filters:
          - name: dateparse
            args: yyyy-MM-ddHH:mm:ss
        date_ago:
          selector: td:nth-child(5):not(:contains("-"))
          optional: true
          filters:
          - name: re_replace
            args:
            - năm
            - 'years '
          - name: re_replace
            args:
            - tháng
            - 'months '
          - name: re_replace
            args:
            - ngày
            - 'days '
          - name: re_replace
            args:
            - phút
            - 'minutes '
          - name: re_replace
            args:
            - giờ
            - 'hours '
          - name: timeago
        date:
          text: '{{ if or .Result.date_year .Result.date_ago }}{{ or .Result.date_year .Result.date_ago }}{{ else }}now{{
            end }}'
        size:
          selector: td:nth-child(6)
        seeders:
          selector: td:nth-child(7)
        leechers:
          selector: td:nth-child(8)
        grabs:
          selector: td:nth-child(9)
        downloadvolumefactor:
          case:
            span.label:contains("Free"): 0
            span.label:contains("50%"): 0.5
            span.label:contains("30%"): 0.7
            '*': 1
        uploadvolumefactor:
          case:
            span.label:contains("2X"): 2
            '*': 1
        minimumratio:
          text: 0.5
        description:
          selector: td:nth-child(3) > div:nth-child(2)
          remove: a, b, font, img, span

# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: anthelion-api
  name: Anthelion (API)
  description: Anthelion (ANT) is a Private Torrent Tracker for MOVIES
  language: en-US
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/anthelion-api.yml
    sourceId: anthelion-api
    license: GPL-2.0-only
    fileSha256: 1fd3183966f8c04d8e1a656524e4749be2ce148f55ce1984282b5d77aa2fd6f1
    contentSha256: 42534a91edf57bf927d54ebf469732e8e8217e4c256e975ce24899df5a048379
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://anthelion.me/
  replacedIds:
  - tehconnectionme
  - anthelion
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 2000
        cat: Movies
      - id: 2030
        cat: Movies/SD
      - id: 2040
        cat: Movies/HD
      - id: 2045
        cat: Movies/UHD
      - id: 2050
        cat: Movies/BluRay
      - id: 2080
        cat: Movies/WEB-DL
      - id: 8000
        cat: Other
      modes:
        search:
        - q
        movie-search:
        - q
        - imdbid
        - tmdbid
    settings:
    - name: apikey
      type: text
      label: APIKey
    - name: info_key
      type: info
      label: About your API key
      default: Find or Generate a new API Key by accessing your <a href="https://anthelion.me/" target="_blank">Anthelion</a>
        account <i>Settings</i> page and scrolling down to the <b>API Key</b> section.<br>Tick the <b>Search</b> and <b>Download</b>
        checkboxes and click the <b>save profile</b> button to generate the key.
    - name: info_activity
      type: info
      label: Account Inactivity
      default: Activity is defined as actually using your account, including the snatch/grab/seeding of torrents. You are
        also required to sign-in to the web frontend of Anthelion at least once per 3mo (90 days) (Note, using autodl/ARR
        does not satisfy this).
    authentication:
      path: api.php
      method: get
      inputs:
        api_key: '{{ .Config.apikey }}'
      error:
      - selector: :root:contains("Unrecognized key")
        message:
          text: The API key was not accepted by {{ .Config.sitelink }}.
      - selector: :root:contains("No API_KEY provided")
        message:
          text: You did not enter an API key.
      - selector: :root:contains("not have MFA")
        message:
          text: User does not have MFA enabled on the account.
    search:
      paths:
      - path: api.php
        response:
          type: json
      inputs:
        api_key: '{{ .Config.apikey }}'
        o: json
        t: movie
        q: '{{ .Keywords }}'
        tmdb: '{{ .Query.TMDBID }}'
        imdb: '{{ .Query.IMDBIDShort }}'
        limit: 100
        offset: 0
      rows:
        selector: item
        count:
          selector: response.total
      fields:
        category:
          selector: category
          filters:
          - name: split
            args:
            - ','
            - -1
          - name: re_replace
            args:
            - 1\d{5}
            - '2000'
        year:
          selector: year
          optional: true
        _codec:
          selector: codec
        _container:
          selector: container
          filters:
          - name: toupper
        _media:
          selector: media
          filters:
          - name: replace
            args:
            - WEB
            - WEB-DL
        _resolution:
          selector: resolution
        _audioFormat:
          selector: audioFormat
        _subbing:
          selector: subbing
          optional: true
        _flags:
          selector: flags
          optional: true
          filters:
          - name: replace
            args:
            - ','
            - ' '
        _language:
          selector: language
          optional: true
        _releaseGroup:
          selector: releaseGroup
          optional: true
          filters:
          - name: trim
        title_optional:
          selector: title
          filters:
          - name: htmldecode
          - name: append
            args: ' {{ .Result.year }} {{ .Result._resolution }} {{ .Result._media }} {{ .Result._container }} {{ .Result._codec
              }} {{ .Result._audioFormat }}{{ if .Result._subbing }} Subs{{ else }}{{ end }}{{ if .Result._flags }} {{ .Result._flags
              }}{{ else }}{{ end }}{{ if .Result._language }} {{ .Result._language }}{{ else }}{{ end }}{{ if and (.Result._releaseGroup)
              (ne .Result._releaseGroup "NULL") }}-{{ .Result._releaseGroup }}{{ else }}{{ end }}'
          - name: replace
            args:
            - M2TS H265
            - M2TS HEVC
          - name: replace
            args:
            - M2TS H264
            - M2TS AVC
        details:
          selector: guid
        download:
          selector: link
          filters:
          - name: htmldecode
        infohash:
          selector: infohash
        imdbid:
          selector: imdb
        tmdbid:
          selector: tmdb
        genre:
          selector: tags
          filters:
          - name: replace
            args:
            - .
            - _
        description:
          text: '{{ .Result.genre }}'
        files:
          selector: fileCount
        seeders:
          selector: seeders
        leechers:
          selector: leechers
        grabs:
          selector: grabs
        date:
          selector: pubDate
        size:
          selector: size
        downloadvolumefactor:
          text: 1
        uploadvolumefactor:
          text: 1
        minimumratio:
          text: 1.0
        minimumseedtime:
          text: 259200
        _filename:
          selector: files[0].name
          filters:
          - name: htmldecode
          optional: true
        title:
          text: '{{ if and (eq .Result.files "1") (.Result._filename) }}{{ .Result._filename }}{{ else }}{{ .Result.title_optional
            }}{{ end }}'

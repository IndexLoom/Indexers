# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: jme-reunit3d-api
  name: JME-REUNIT3D (API)
  description: JME-REUNIT3D is a GERMAN Private Torrent Tracker for MOVIES / TV / GENERAL
  language: de-DE
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/jme-reunit3d-api.yml
    sourceId: jme-reunit3d-api
    license: GPL-2.0-only
    fileSha256: 52fee596686448f9864e90eef67f9f30f899cdec3bd564b538534b58fa1ad971
    contentSha256: 8d9e646b447fc06c51a833da84e58d9c35b811e3e16678640046c082d8348ad8
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://jme-reunit3d.de/
  legacyUrls:
  - https://jme-reunit3d.eu/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 1
        cat: Movies
        desc: Movies
      - id: 2
        cat: TV
        desc: TV
      - id: 6
        cat: XXX
        desc: XXX
      - id: 7
        cat: TV/Sport
        desc: Sport
      - id: 3
        cat: Audio
        desc: Music
      - id: 5
        cat: PC
        desc: Application
      - id: 4
        cat: Console
        desc: Game
      - id: 8
        cat: Books
        desc: E-Books
      - id: 9
        cat: Audio/Audiobook
        desc: Audio-Books
      - id: 12
        cat: Audio/Video
        desc: Music-Video
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        - tvdbid
        - tmdbid
        movie-search:
        - q
        - imdbid
        - tmdbid
        music-search:
        - q
        book-search:
        - q
    settings:
    - name: apikey
      type: text
      label: APIKey
    - name: info_key
      type: info
      label: About your API key
      default: Find or Generate a new API Token by accessing your <a href="https://jme-reunit3d.de/" target="_blank">JME-REUNIT3D</a>
        account <i>My Security</i> page and clicking on the <b>API Token</b> tab.
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: created_at
      options:
        created_at: created
        seeders: seeders
        size: size
        name: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_activity
      type: info
      label: Account Inactivity
      default: If a user has not actively logged in for 30 days, the profile is automatically deactivated and receives the
        user class 'Disabled'.<br>After deactivation, the profile can be reactivated within 60 days – after which it will
        be permanently deleted.
    authentication:
      path: api/torrents
      method: get
      error:
      - selector: a[href*="/login"]
        message:
          text: The API key was not accepted by {{ .Config.sitelink }}.
      - selector: :root:contains("Account is Banned")
    search:
      paths:
      - path: api/torrents/filter
        response:
          type: json
      headers:
        Authorization:
        - Bearer {{ .Config.apikey }}
      inputs:
        $raw: '{{ range .Categories }}&categories[]={{.}}{{end}}'
        name: '{{ .Keywords }}'
        imdbId: '{{ .Query.IMDBIDShort }}'
        tmdbId: '{{ .Query.TMDBID }}'
        tvdbId: '{{ .Query.TVDBID }}'
        free: '{{ if .Config.freeleech }}1{{ else }}{{ end }}'
        sortField: '{{ .Config.sort }}'
        sortDirection: '{{ .Config.type }}'
        perPage: 100
      keywordsfilters:
      - name: re_replace
        args:
        - \.
        - ' '
      rows:
        selector: data
        attribute: attributes
      fields:
        categorydesc:
          selector: category
        title:
          selector: name
        details:
          selector: details_link
        download:
          selector: download_link
        poster:
          selector: poster
          filters:
          - name: replace
            args:
            - https://via.placeholder.com/90x135
            - ''
        imdbid:
          selector: imdb_id
        tmdbid:
          selector: tmdb_id
        tvdbid:
          selector: tvdb_id
        files:
          selector: num_file
        seeders:
          selector: seeders
        leechers:
          selector: leechers
        grabs:
          selector: times_completed
        date:
          selector: created_at
          filters:
          - name: append
            args: ' +00:00'
          - name: dateparse
            args: MM/dd/yyyy HH:mm:ss zzz
        size:
          selector: size
        downloadvolumefactor:
          selector: freeleech
          case:
            0%: 1
            25%: 0.75
            50%: 0.5
            75%: 0.25
            100%: 0
            '*': 0
        uploadvolumefactor:
          selector: double_upload
          case:
            0: 1
            1: 2
        minimumseedtime:
          text: 345600

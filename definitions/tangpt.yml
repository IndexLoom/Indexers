# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: tangpt
  name: TangPT (躺平)
  description: TangPT (躺平) is a CHINESE Private Torrent Tracker for MOVIES / TV / GENERAL
  language: zh-CN
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/tangpt.yml
    sourceId: tangpt
    license: GPL-2.0-only
    fileSha256: 4b0f9916ecd15426106c499ef9c13f2e817796de61619c997f6955d8e6e9474b
    contentSha256: 207ca1cc19886f88de2d1fcefc3b0890862c00de3d5e870613d996f8d8214e76
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://www.tangpt.top/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 401
        cat: Movies
        desc: 电影
      - id: 402
        cat: TV
        desc: 电视剧
      - id: 403
        cat: TV
        desc: 综艺
      - id: 404
        cat: TV/Documentary
        desc: 纪录片
      - id: 405
        cat: TV/Anime
        desc: 动漫
      - id: 406
        cat: Audio/Video
        desc: MV
      - id: 407
        cat: TV/Sport
        desc: 体育
      - id: 409
        cat: Audio/Lossless
        desc: 音乐
      - id: 414
        cat: Other
        desc: 其他
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        - doubanid
        movie-search:
        - q
        - imdbid
        - doubanid
        music-search:
        - q
    settings:
    - name: cookie
      type: text
      label: Cookie
    - name: info_cookie
      type: info_cookie
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: 4
      options:
        4: created
        7: seeders
        5: size
        1: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_tpp
      type: info
      label: Results Per Page
      default: For best results, change the <b>Torrents per page:</b> setting to <b>100</b> on your account profile.
    - name: info_activity
      type: info
      label: Account Inactivity
      default: Cherish your user account. Inactive accounts would be deleted based on the following rules:<ol><li>Veteran
        User or above would never be deleted</li><li>Elite User or above would never be deleted if parked (at User CP)</li><li>Parked
        accounts would be deleted if users have not logged in for more than 400 days in a row</li><li>Un-parked accounts would
        be deleted if users have not logged in for more than 150 days in a row</li><li>Accounts with both uploaded and downloaded
        amount being 0 would be deleted if users have not logged in for more than 100 days in a row.</li></ol>
    authentication:
      method: cookie
      inputs:
        cookie: '{{ .Config.cookie }}'
      test:
        path: index.php
        selector: a[href="logout.php"]
    search:
      paths:
      - path: torrents.php
      inputs:
        $raw: '{{ range .Categories }}cat{{.}}=1&{{end}}'
        search: '{{ if .Query.IMDBID }}{{ .Query.IMDBID }}{{ else }}{{ end }}{{ if and .Query.DoubanID (eq .Query.IMDBID .False)
          }}{{ .Query.DoubanID }}{{ else }}{{ end }}{{ if and (eq .Query.IMDBID .False) (eq .Query.DoubanID .False) }}{{ .Keywords
          }}{{ else }}{{ end }}'
        incldead: 0
        spstate: '{{ if .Config.freeleech }}2{{ else }}0{{ end }}'
        search_area: '{{ if .Query.IMDBID }}4{{ else }}{{ end }}{{ if and .Query.DoubanID (eq .Query.IMDBID .False) }}1{{
          else }}{{ end }}{{ if and (eq .Query.IMDBID .False) (eq .Query.DoubanID .False) }}0{{ else }}{{ end }}'
        search_mode: 0
        sort: '{{ .Config.sort }}'
        type: '{{ .Config.type }}'
        notnewword: 1
      rows:
        selector: table.torrents > tbody > tr:has(table.torrentname)
      fields:
        category:
          selector: a[href^="?cat="]
          attribute: href
          filters:
          - name: querystring
            args: cat
        title_default:
          selector: a[href^="details.php?id="]
        title:
          selector: a[title][href^="details.php?id="]
          attribute: title
          optional: true
          default: '{{ .Result.title_default }}'
        details:
          selector: a[href^="details.php?id="]
          attribute: href
        download:
          selector: a[href^="download.php?id="]
          attribute: href
        poster:
          selector: img[data-src]
          attribute: data-src
        imdbid:
          selector: a[href*="imdb.com/title/tt"]
          attribute: href
        doubanid:
          selector: a[href*="movie.douban.com/subject/"]
          attribute: href
        date_elapsed:
          selector: td.rowfollow:nth-child(4) > span[title]
          attribute: title
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-dd HH:mm:ss zzz
        date_added:
          selector: td.rowfollow:nth-child(4):not(:has(span))
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-ddHH:mm:ss zzz
        date:
          text: '{{ if or .Result.date_elapsed .Result.date_added }}{{ or .Result.date_elapsed .Result.date_added }}{{ else
            }}now{{ end }}'
        size:
          selector: td.rowfollow:nth-child(5)
          optional: true
          default: 512MB
        seeders:
          selector: td.rowfollow:nth-child(6)
          optional: true
          default: 0
        leechers:
          selector: td.rowfollow:nth-child(7)
          optional: true
          default: 0
        grabs:
          selector: td.rowfollow:nth-child(8)
          optional: true
          default: 0
        downloadvolumefactor:
          case:
            img.pro_free: 0
            img.pro_free2up: 0
            img.pro_50pctdown: 0.5
            img.pro_50pctdown2up: 0.5
            img.pro_30pctdown: 0.3
            '*': 1
        uploadvolumefactor:
          case:
            img.pro_50pctdown2up: 2
            img.pro_free2up: 2
            img.pro_2up: 2
            '*': 1
        description_verified:
          case:
            span[title="通过"], span[title="通過"], span[title="Allowed"]: 'Verified:'
            span[title="未审"], span[title="未審"], span[title="Not reviewed"]: 'Unverified:'
            span[title="拒绝"], span[title="拒絕"], span[title="Denied"]: 'Banned:'
        description:
          selector: td.rowfollow:nth-child(2)
          remove: a, b, font, img, span
          filters:
          - name: prepend
            args: '{{ .Result.description_verified }} '

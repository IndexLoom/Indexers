# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: pandacd
  name: PandaCD
  description: PandaCD is a Public Tracker for MUSIC (Creative Commons and artist-permitted content)
  language: en-US
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/pandacd.yml
    sourceId: pandacd
    license: GPL-2.0-only
    fileSha256: ccdbcf3453487cecda48f1dd7ae97ad5d4ce9f48b438ceb19f27ffcc8b452b26
    contentSha256: 2cc493ef8f6479d63f805a4fdf1a96d0f1c1ab8173b29089e89a6755d2da4025
spec:
  protocol: torrent
  privacy: public
  baseUrls:
  - https://pandacd.io/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 3000
        cat: Audio
        desc: Audio
      - id: 3010
        cat: Audio/MP3
        desc: MP3
      - id: 3040
        cat: Audio/Lossless
        desc: Lossless
      modes:
        search:
        - q
        music-search:
        - q
        - artist
        - album
        - genre
    settings: []
    search:
      paths:
      - path: api/torznab
        response:
          type: xml
      inputs:
        t: '{{ .Query.Type }}'
        q: '{{ .Keywords }}'
        cat: '{{ join .Categories "," }}'
        artist: '{{ .Query.Artist }}'
        album: '{{ .Query.Album }}'
        genre: '{{ .Query.Genre }}'
        limit: 100
      rows:
        selector: rss > channel > item
      fields:
        category:
          selector: '[name=category]'
          attribute: value
        title:
          selector: title
        details:
          selector: guid
        download:
          selector: enclosure
          attribute: url
        date:
          selector: pubDate
          filters:
          - name: dateparse
            args: ddd, dd MMM yyyy HH:mm:ss zzz
        size:
          selector: '[name=size]'
          attribute: value
        seeders:
          selector: '[name=seeders]'
          attribute: value
        leechers:
          selector: '[name=leechers]'
          attribute: value
        grabs:
          selector: '[name=grabs]'
          attribute: value
        files:
          selector: '[name=files]'
          attribute: value
        infohash:
          selector: '[name=infohash]'
          attribute: value
        genre:
          selector: '[name=genre]'
          attribute: value
        description:
          text: '{{ .Result.genre }}'
        downloadvolumefactor:
          selector: '[name=downloadvolumefactor]'
          attribute: value
        uploadvolumefactor:
          selector: '[name=uploadvolumefactor]'
          attribute: value

# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: pttey
  name: PTTey
  description: PTTey is a CHINESE Private Torrent Tracker for MOVIES / TV / GENERAL
  language: zh-CN
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/pttey.yml
    sourceId: pttey
    license: GPL-2.0-only
    fileSha256: feca085b32fa9691db081e59b70617c9250009dcf269f1715f155b53f3d7f1d3
    contentSha256: a8e965684b8c853d61ab74fb5a4c30b0e768f434a51a3e0cd553fe30dc1a36cc
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://pt.tey.cc/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 401
        cat: Movies
        desc: Movies(电影)
      - id: 402
        cat: TV
        desc: TV Series(电视剧)
      - id: 404
        cat: TV/Documentary
        desc: Documentaries(记录片)
      - id: 403
        cat: TV/Other
        desc: TV Shows(电视节目)
      - id: 405
        cat: TV/Anime
        desc: Animations(动漫)
      - id: 407
        cat: TV/Sport
        desc: Sports(体育)
      - id: 406
        cat: Audio/Video
        desc: Music Videos(MV)
      - id: 408
        cat: Audio
        desc: Music(音乐)
      - id: 409
        cat: Other
        desc: Misc(其他)
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        movie-search:
        - q
        - imdbid
        music-search:
        - q
    settings:
    - name: cookie
      type: text
      label: Cookie
    - name: info_cookie
      type: info_cookie
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: 4
      options:
        4: created
        7: seeders
        5: size
        1: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_tpp
      type: info
      label: Results Per Page
      default: For best results, change the <b>Torrents per page:</b> setting to <b>100</b> on your account profile.
    - name: info_activity
      type: info
      label: Account Inactivity
      default: Cherish your user account. Inactive accounts would be deleted based on the following rules:<ol><li>Veteran
        User or above would never be deleted.</li><li>Elite User or above would never be deleted if parked (at User CP).</li><li>Parked
        accounts would be deleted if users have not logged in for more than 400 days in a row.</li><li>Unparked accounts would
        be deleted if users have not logged in for more than 150 days in a row.</li><li>Accounts with both uploaded and downloaded
        amount being 0 would be deleted if users have not logged in for more than 100 days in a row.</li></ol>
    authentication:
      method: cookie
      inputs:
        cookie: '{{ .Config.cookie }}'
      test:
        path: index.php
        selector: a[href*="logout.php"]
    search:
      paths:
      - path: torrents.php
      inputs:
        $raw: '{{ range .Categories }}cat{{.}}=1&{{end}}'
        search: '{{ if .Query.IMDBID }}{{ .Query.IMDBID }}{{ else }}{{ .Keywords }}{{ end }}'
        incldead: 0
        spstate: '{{ if .Config.freeleech }}2{{ else }}0{{ end }}'
        search_area: '{{ if .Query.IMDBID }}4{{ else }}0{{ end }}'
        search_mode: 0
        sort: '{{ .Config.sort }}'
        type: '{{ .Config.type }}'
        notnewword: 1
      rows:
        selector: table.torrents > tbody > tr:has(a[href*="download.php?id="])
      fields:
        category:
          selector: a[href*="?cat="]
          attribute: href
          filters:
          - name: querystring
            args: cat
        title_default:
          selector: table.torrentname td a[href^="details.php?id="]
        title:
          selector: a[title][href*="details.php?id="]
          attribute: title
          optional: true
          default: '{{ .Result.title_default }}'
        details:
          selector: a[href*="details.php?id="]
          attribute: href
        download:
          selector: a[href*="download.php?id="]
          attribute: href
        poster:
          selector: img[data-src]
          attribute: data-src
        date_elapsed:
          selector: td.rowfollow:nth-child(4) > span[title]
          attribute: title
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-dd HH:mm:ss zzz
        date_added:
          selector: td.rowfollow:nth-child(4):not(:has(span))
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-ddHH:mm:ss zzz
        date:
          text: '{{ if or .Result.date_elapsed .Result.date_added }}{{ or .Result.date_elapsed .Result.date_added }}{{ else
            }}now{{ end }}'
        size:
          selector: td.rowfollow:nth-child(5)
          optional: true
          default: 512MB
        seeders:
          selector: td.rowfollow:nth-child(6)
          optional: true
          default: 0
        leechers:
          selector: td.rowfollow:nth-child(7)
          optional: true
          default: 0
        grabs:
          selector: td.rowfollow:nth-child(8)
          optional: true
          default: 0
        downloadvolumefactor:
          case:
            img.pro_free: 0
            img.pro_free2up: 0
            img.pro_50pctdown: 0.5
            img.pro_50pctdown2up: 0.5
            img.pro_30pctdown: 0.3
            '*': 1
        uploadvolumefactor:
          case:
            img.pro_50pctdown2up: 2
            img.pro_free2up: 2
            img.pro_2up: 2
            '*': 1

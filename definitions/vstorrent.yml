# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: vstorrent
  name: VSTorrent
  description: VSTorrent is a Public site for AUDIO apps, plugins and samples
  language: en-US
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/vstorrent.yml
    sourceId: vstorrent
    license: GPL-2.0-only
    fileSha256: febde4fde114fdf74150a8b67594f5148b840bae257b5652c5fcef3c92707dbd
    contentSha256: ebfa05f205d53d9b93d3d2dad1d8341a5e26b1f1a4be2aeef1c677345192df4a
spec:
  protocol: torrent
  privacy: public
  baseUrls:
  - https://vstorrent.org/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categories:
        PC: PC
      modes:
        search:
        - q
    settings:
    - name: sort
      type: select
      label: Sort requested from site
      default: date
      options:
        date: created
        relevance: relevance
    - name: type
      type: select
      label: Order requested from site
      default: DESC
      options:
        DESC: desc
        ASC: asc
    download:
      selectors:
      - selector: a[href^="magnet:?xt"]
        attribute: href
    search:
      paths:
      - path: /
      allowEmptyInputs: true
      inputs:
        s: '{{ .Keywords }}'
        orderby: '{{ .Config.sort }}'
        order: '{{ .Config.type }}'
        post_type: post
      rows:
        selector: article:has(a)
        filters:
        - name: andmatch
      fields:
        category:
          text: PC
        title:
          selector: .entry-title
        details:
          selector: .entry-title > a
          attribute: href
        download:
          selector: .entry-title > a
          attribute: href
        description:
          selector: .entry-summary
        date:
          selector: span.date
        size:
          text: 512 MB
        seeders:
          text: 1
        leechers:
          text: 1
        downloadvolumefactor:
          text: 0
        uploadvolumefactor:
          text: 1

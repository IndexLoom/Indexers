# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: itatorrents
  name: ItaTorrents
  description: ItaTorrents is an ITALIAN Private tracker for MOVIES / TV / GENERAL
  language: it-IT
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/itatorrents.yml
    sourceId: itatorrents
    license: GPL-2.0-only
    fileSha256: 9058cb9e2554e3a6ab842105a1018216bbccc58e85ac6666dc2d5c857174033c
    contentSha256: ed3d9bc6fccbdb8d598782b528e3bdb52db6a3e887640752e6692024b3421749
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://itatorrents.xyz/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 1
        cat: Movies
        desc: Film
      - id: 2
        cat: TV
        desc: Serie TV
      - id: 3
        cat: Audio
        desc: Musica
      - id: 4
        cat: Console
        desc: Giochi
      - id: 5
        cat: PC
        desc: Applicazioni
      - id: 6
        cat: Books/Mags
        desc: Edicola
      - id: 7
        cat: Books/EBook
        desc: E-Book
      - id: 8
        cat: Audio/Audiobook
        desc: AudioLibri
      - id: 12
        cat: Other
        desc: Corsi
      - id: 13
        cat: Books/Comics
        desc: Fumetti
      - id: 14
        cat: Audio/Video
        desc: Concerti
      - id: 15
        cat: Audio
        desc: Discografie
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        - tvdbid
        - tmdbid
        movie-search:
        - q
        - imdbid
        - tmdbid
        music-search:
        - q
        book-search:
        - q
    settings:
    - name: apikey
      type: text
      label: APIKey
    - name: info_key
      type: info
      label: About your API key
      default: Find or Generate a new API Token by accessing your <a href="https://itatorrents.xyz/" target="_blank">ItaTorrents</a>
        account <i>My Settings</i> page and clicking on the <b>API Key</b> tab.
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: tmdbidonly
      type: checkbox
      label: Disable IMDB and TVDB ID search (only support TMDB ID) to potentially improve Sonarr and Radarr results
      default: false
    - name: single_file_release_use_filename
      type: checkbox
      label: Use filename as title for single file releases
      default: true
    - name: sort
      type: select
      label: Sort requested from site
      default: created_at
      options:
        created_at: created
        seeders: seeders
        size: size
        name: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_activity
      type: info
      label: Account Inactivity
      default: To not get disabled, you are required to login to the site at least once every 90 days or be actively seeding
        at least 1 torrent.
    authentication:
      path: api/torrents
      method: get
      error:
      - selector: a[href*="/login"]
        message:
          text: The API key was not accepted by {{ .Config.sitelink }}.
      - selector: :root:contains("Account is Banned")
    search:
      paths:
      - path: api/torrents/filter
        response:
          type: json
      headers:
        Authorization:
        - Bearer {{ .Config.apikey }}
      inputs:
        $raw: '{{ range .Categories }}&categories[]={{.}}{{end}}'
        name: '{{ .Keywords }}'
        seasonNumber: '{{ .Query.Season }}'
        episodeNumber: '{{ .Query.Ep }}'
        imdbId: '{{ if .Config.tmdbidonly }}{{ else }}{{ .Query.IMDBIDShort }}{{ end }}'
        tmdbId: '{{ .Query.TMDBID }}'
        tvdbId: '{{ if .Config.tmdbidonly }}{{ else }}{{ .Query.TVDBID }}{{ end }}'
        free[]: '{{ if .Config.freeleech }}100{{ else }}{{ end }}'
        sortField: '{{ .Config.sort }}'
        sortDirection: '{{ .Config.type }}'
        perPage: 100
      keywordsfilters:
      - name: re_replace
        args:
        - \.
        - ' '
      - name: re_replace
        args:
        - ^([SE]\d{1,4}){1,2}$
        - '{{ if or (.Query.Season) (.Query.Ep) }}{{ else }}$1{{ end }}'
      - name: re_replace
        args:
        - (?i)\bS0*(\d+)\b
        - Stagione $1
      rows:
        selector: data
        attribute: attributes
      fields:
        category:
          selector: category_id
        title_optional:
          selector: name
        title_filename:
          selector: files[0].name
          optional: true
        files:
          selector: num_file
        title:
          text: '{{ if and (.Config.single_file_release_use_filename) (eq .Result.files "1") (.Result.title_filename) }}{{
            .Result.title_filename }}{{ else }}{{ .Result.title_optional }}{{ end }}'
          filters:
          - name: re_replace
            args:
            - \.
            - ' '
          - name: re_replace
            args:
            - (?i)\bStagion[ei]\s?(\d{1})\b|\bSeason'?s?\s?(\d{1})\b
            - S0$1$2
          - name: re_replace
            args:
            - (?i)\bStagion[ei]\s?(\d{2,})\b|\bSeason'?s?\s?(\d{2,})\b
            - S$1$2
        details:
          selector: details_link
        download:
          selector: download_link
        poster:
          selector: meta.poster
          filters:
          - name: replace
            args:
            - https://via.placeholder.com/90x135
            - ''
        imdbid:
          selector: imdb_id
        tmdbid:
          selector: tmdb_id
        tvdbid:
          selector: tvdb_id
        genre:
          selector: meta.genres
          filters:
          - name: re_replace
            args:
            - (?i)(televisione film)
            - televisione_film
          - name: replace
            args:
            - ' & '
            - _&_
        _internal:
          selector: internal
          case:
            false: '{{ .False }}'
            true: '{{ .True }}'
        description:
          text: '{{ if .Result._internal }}Internal{{ else }}{{ end }}{{ if and .Result._internal .Result.genre }} | {{ else
            }}{{ end }}{{ .Result.genre }}'
        seeders:
          selector: seeders
        leechers:
          selector: leechers
        grabs:
          selector: times_completed
        date:
          selector: created_at
          filters:
          - name: append
            args: ' +00:00'
          - name: dateparse
            args: MM/dd/yyyy HH:mm:ss zzz
        size:
          selector: size
        _featured:
          selector: featured
          case:
            false: '{{ .False }}'
            true: '{{ .True }}'
        downloadvolumefactor_freeleech:
          selector: freeleech
          case:
            0%: 1
            25%: 0.75
            50%: 0.5
            75%: 0.25
            100%: 0
            '*': 0
        downloadvolumefactor:
          text: '{{ if .Result._featured }}0{{ else }}{{ .Result.downloadvolumefactor_freeleech }}{{ end }}'
        uploadvolumefactor_double_upload:
          selector: double_upload
          case:
            false: 1
            true: 2
        uploadvolumefactor:
          text: '{{ if .Result._featured }}2{{ else }}{{ .Result.uploadvolumefactor_double_upload }}{{ end }}'
        minimumseedtime:
          text: 604800

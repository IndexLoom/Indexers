# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: abnormal-api
  name: Abnormal (API)
  description: ABNormal (ABN) is a FRENCH Private Torrent Tracker for MOVIES / TV / GENERAL
  language: fr-FR
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/abnormal-api.yml
    sourceId: abnormal-api
    license: GPL-2.0-only
    fileSha256: 514c375efed6be41d6d01e37cfff53be0bf5702da1b765587d7f137ecc0efcf1
    contentSha256: af2bf1e2ab5e8b5c65883d739ecc148fdedf8c8f059b9463c28e0739ec2f80ae
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://abn.lol/
  legacyUrls:
  - https://abnormal.ws
  implementation:
    engine: cardigann
    encoding: UTF-8
    requestDelaySeconds: 2.1
    capabilities:
      categorymappings:
      - id: 1
        cat: TV
        desc: Series
      - id: 2
        cat: Movies
        desc: Movies
      - id: 3
        cat: TV/Documentary
        desc: Documentaries
      - id: 4
        cat: TV/Anime
        desc: Anime
      - id: 5
        cat: PC/Games
        desc: Games
      - id: 6
        cat: PC
        desc: Applications
      - id: 7
        cat: Books/EBook
        desc: Ebooks
      - id: 9
        cat: TV
        desc: Emissions
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        movie-search:
        - q
        book-search:
        - q
    settings:
    - name: apikey
      type: text
      label: APIKey
    - name: info_key
      type: info
      label: About your API key
      default: Find your API key on your <a href="https://abn.lol/User/Parameters" target="_blank">Parameters</a> page. The
        API key is read-only and never expires.
    - name: apiurl
      label: API URL
      type: text
      default: api.abn.lol
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: multilang
      type: checkbox
      label: Replace MULTi by another language in release name
      default: false
    - name: multilanguage
      type: select
      label: Replace MULTi by this language
      default: FRENCH
      options:
        FRENCH: FRENCH
        MULTi.FRENCH: MULTi.FRENCH
        ENGLISH: ENGLISH
        MULTi.ENGLISH: MULTi.ENGLISH
        VOSTFR: VOSTFR
        MULTi.VOSTFR: MULTi.VOSTFR
    - name: vostfr
      type: checkbox
      label: Replace VOSTFR and SUBFRENCH with ENGLISH
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: Created
      options:
        Created: created
        Seeders: seeders
        Size: size
        ReleaseName: title
    - name: type
      type: select
      label: Order requested from site
      default: DESC
      options:
        DESC: desc
        ASC: asc
    - name: info_activity
      type: info
      label: Account Inactivity
      default: Accounts are automatically deactivated after 4 months of inactivity (no connection to the site).
    authentication:
      path: https://{{ .Config.apiurl }}/api/Release/Search?name=test&resultByPage=1
      method: get
      error:
      - selector: :root:contains("Unauthorized")
        message:
          text: The API key was not accepted by {{ .Config.sitelink }}.
    search:
      paths:
      - path: https://{{ .Config.apiurl }}/api/Release/Search
        response:
          type: json
      headers:
        Authorization:
        - Bearer {{ .Config.apikey }}
      inputs:
        $raw: '{{ range .Categories }}categoryId={{.}}&{{end}}'
        name: '{{ .Keywords }}'
        resultByPage: 100
        sort: '{{ .Config.sort }}'
        sortDirection: '{{ .Config.type }}'
        freeleech: '{{ if .Config.freeleech }}true{{ else }}{{ end }}'
      keywordsfilters:
      - name: re_replace
        args:
        - \.
        - ' '
      rows:
        selector: $
      fields:
        category:
          selector: categoryId
        title_phase1:
          selector: name
        title_vostfr:
          text: '{{ .Result.title_phase1 }}'
          filters:
          - name: re_replace
            args:
            - (?i)\b(vostfr|subfrench)\b
            - ENGLISH
        title_phase2:
          text: '{{ if .Config.vostfr }}{{ .Result.title_vostfr }}{{ else }}{{ .Result.title_phase1 }}{{ end }}'
        title_multilang:
          text: '{{ .Result.title_phase2 }}'
          filters:
          - name: re_replace
            args:
            - (?i)\b(MULTI(?!.*(?:FRENCH|ENGLISH|VOSTFR)))\b
            - '{{ .Config.multilanguage }}'
        title:
          text: '{{ if .Config.multilang }}{{ .Result.title_multilang }}{{ else }}{{ .Result.title_phase2 }}{{ end }}'
        _id:
          selector: id
        details:
          text: '{{ .Config.sitelink }}Torrent/Details?ReleaseId={{ .Result._id }}'
        download:
          selector: downloadUrl
        infohash:
          selector: infoHash
        date:
          selector: created
          filters:
          - name: append
            args: ' +00:01'
          - name: dateparse
            args: MM/dd/yyyy HH:mm:ss zzz
        size:
          selector: size
        seeders:
          selector: seeders
        leechers:
          selector: leechers
        grabs:
          selector: completed
        downloadvolumefactor:
          selector: freeleech
          case:
            false: 1
            true: 0
        uploadvolumefactor:
          text: 1
        minimumratio:
          text: 1.0
        minimumseedtime:
          text: 345600

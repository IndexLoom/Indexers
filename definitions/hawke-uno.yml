# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: hawke-uno
  name: hawke-uno
  description: hawke-uno (HUNO) is a Private Torrent Tracker for HD MOVIES / TV
  language: en-US
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/hawke-uno.yml
    sourceId: hawke-uno
    license: GPL-2.0-only
    fileSha256: dbbeb940986cb996a4f8ef87de79d0e25fd7e722aa98e67b97b8bb9c7087f83c
    contentSha256: 512e4e595043c8956480efbeb8540a74b89392181c9ed64ec96571dc6776af65
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://hawke.uno/
  legacyUrls:
  - https://www.hawke.uno/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 1
        cat: Movies
        desc: Movie
      - id: 2
        cat: TV
        desc: TV
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        - tvdbid
        - tmdbid
        movie-search:
        - q
        - imdbid
        - tmdbid
    settings:
    - name: apikey
      type: text
      label: APIKey
    - name: info_key
      type: info
      label: About your API key
      default: Find or Generate a new API Token by accessing your <a href="https://www.hawke.uno/" target="_blank">hawke-uno</a>
        account <i>My Security</i> page and clicking on the <b>API Token</b> tab.
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: created_at
      options:
        created_at: created
        seeders: seeders
        size: size
        name: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_activity
      type: info
      label: Account Inactivity
      default: SS and IF members must either login or register any seeding activity in a rolling window of 180 days to retain
        their account. Failure to do so will result in their account being disabled by the site automatically.
    authentication:
      path: api/torrents
      method: get
      error:
      - selector: a[href*="/login"]
        message:
          text: The API key was not accepted by {{ .Config.sitelink }}.
      - selector: :root:contains("Account is Banned")
    search:
      paths:
      - path: api/torrents/filter
        response:
          type: json
      headers:
        Authorization:
        - Bearer {{ .Config.apikey }}
      inputs:
        $raw: '{{ range .Categories }}&categories[]={{.}}{{end}}'
        name: '{{ .Keywords }}'
        imdbId: '{{ .Query.IMDBIDShort }}'
        tmdbId: '{{ .Query.TMDBID }}'
        tvdbId: '{{ .Query.TVDBID }}'
        free: '{{ if .Config.freeleech }}1{{ else }}{{ end }}'
        sortField: '{{ .Config.sort }}'
        sortDirection: '{{ .Config.type }}'
        perPage: 100
      keywordsfilters:
      - name: re_replace
        args:
        - \.
        - ' '
      rows:
        selector: data
        attribute: attributes
      fields:
        categorydesc:
          selector: category
        title:
          selector: name
          filters:
          - name: re_replace
            args:
            - \(((?>[^()]+|(?<o>)\(|(?<-o>)\))*(?(o)(?!)))(?<!\bHONE)(?<=\s*- [\w]+)\)$
            - $1
          - name: re_replace
            args:
            - \s*-\s+(\w+)$
            - -$1
        details:
          selector: details_link
        download:
          selector: download_link
        poster:
          selector: poster
          filters:
          - name: replace
            args:
            - https://via.placeholder.com/90x135
            - ''
        imdbid:
          selector: imdb_id
        tmdbid:
          selector: tmdb_id
        tvdbid:
          selector: tvdb_id
        files:
          selector: num_file
        seeders:
          selector: seeders
        leechers:
          selector: leechers
        grabs:
          selector: times_completed
        date:
          selector: created_at
          filters:
          - name: append
            args: ' +00:00'
          - name: dateparse
            args: MM/dd/yyyy HH:mm:ss zzz
        size:
          selector: size
        _internal:
          selector: internal
          case:
            0: '{{ .False }}'
            1: '{{ .True }}'
        description:
          text: '{{ if .Result._internal }}Internal{{ else }}{{ end }}'
        downloadvolumefactor:
          selector: freeleech
          case:
            0: 1
            1: 0
        uploadvolumefactor:
          selector: double_upload
          case:
            0: 1
            1: 2
        minimumseedtime:
          text: 432000

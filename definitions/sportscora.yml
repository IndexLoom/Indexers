# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: sportscora
  name: SportsCora (API)
  description: SportsCora is a Private Torrent Tracker for SPORTS
  language: en-US
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/sportscora.yml
    sourceId: sportscora
    license: GPL-2.0-only
    fileSha256: 2a765143623d3758733a24daee59dabde8d9599068da14d1e08ead6331b045a4
    contentSha256: 343f7b31c54d333616ea540866b6900fe7667691460d55a8a53a0e724cc1e9d7
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://sportscora.com/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 1
        cat: Movies
        desc: Movie
      - id: 2
        cat: TV
        desc: TV
      - id: 3
        cat: Audio
        desc: Music
      - id: 4
        cat: Console
        desc: Game
      - id: 5
        cat: PC
        desc: Application
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        - tvdbid
        - tmdbid
        movie-search:
        - q
        - imdbid
        - tmdbid
        music-search:
        - q
    settings:
    - name: apikey
      type: text
      label: APIKey
    - name: info_key
      type: info
      label: About your API key
      default: Find or Generate a new API Token by accessing your <a href="https://sportscora.com/" target="_blank">SportsCora</a>
        account <i>My Security</i> page and clicking on the <b>API Token</b> tab.
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: created_at
      options:
        created_at: created
        seeders: seeders
        size: size
        name: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    authentication:
      path: api/torrents
      method: get
      error:
      - selector: a[href*="/login"]
        message:
          text: The API key was not accepted by {{ .Config.sitelink }}.
      - selector: :root:contains("Account is Banned")
    search:
      paths:
      - path: api/torrents/filter
        response:
          type: json
      headers:
        Authorization:
        - Bearer {{ .Config.apikey }}
      inputs:
        $raw: '{{ range .Categories }}&categories[]={{.}}{{end}}'
        name: '{{ .Keywords }}'
        imdbId: '{{ .Query.IMDBIDShort }}'
        tmdbId: '{{ .Query.TMDBID }}'
        tvdbId: '{{ .Query.TVDBID }}'
        free: '{{ if .Config.freeleech }}1{{ else }}{{ end }}'
        sortField: '{{ .Config.sort }}'
        sortDirection: '{{ .Config.type }}'
        perPage: 100
      keywordsfilters:
      - name: re_replace
        args:
        - \.
        - ' '
      rows:
        selector: data
        attribute: attributes
      fields:
        _id:
          selector: ..id
        categorydesc:
          selector: category
        title:
          selector: name
        details:
          text: '{{ .Config.sitelink }}torrents/{{ .Result._id }}'
        download:
          selector: download_link
        imdbid:
          selector: imdb_id
        tmdbid:
          selector: tmdb_id
        tvdbid:
          selector: tvdb_id
        files:
          selector: num_file
        seeders:
          selector: seeders
        leechers:
          selector: leechers
        grabs:
          selector: times_completed
        date:
          selector: created_at
          filters:
          - name: append
            args: ' +00:00'
          - name: dateparse
            args: MM/dd/yyyy HH:mm:ss zzz
        size:
          selector: size
        downloadvolumefactor:
          selector: freeleech
          case:
            'No': 1
            'Yes': 0
        uploadvolumefactor:
          selector: double_upload
          case:
            'No': 1
            'Yes': 2
        minimumseedtime:
          text: 86400

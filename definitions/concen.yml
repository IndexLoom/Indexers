# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: concen
  name: ConCen
  description: ConCen (Conspiracy Central) is a Private conspiracy related torrent index
  language: en-US
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/concen.yml
    sourceId: concen
    license: GPL-2.0-only
    fileSha256: 17287363af96f5d815e488c896793822676cc9415fc1a1fde29deb03de5e8773
    contentSha256: 05fc2c4ee13e9084dc3b02e0e7b52dc9a9c8c9e69160f80493a43956a1c239d0
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://concen.org/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categories:
        1: Other
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        movie-search:
        - q
        book-search:
        - q
    settings:
    - name: sort
      type: select
      label: Sort requested from site
      default: created
      options:
        created: created
        seeds: seeders
        size: size
        title: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    search:
      paths:
      - path: torrents
      inputs:
        title_op: allwords
        title: '{{ .Keywords }}'
        title_1_op: not
        title_1: ''
        seeds: All
        order: '{{ .Config.sort }}'
        sort: '{{ .Config.type }}'
      rows:
        selector: table > tbody > tr:has(td.views-field-title)
      fields:
        title:
          selector: td.views-field-title a
        category:
          text: 1
        details:
          selector: td.views-field-title a
          attribute: href
        download:
          selector: td.views-field-field-torrent a
          attribute: href
        magnet:
          selector: td.views-field-name a
          attribute: href
        date:
          selector: td.views-field-created
        size:
          selector: td.views-field-size
        seeders:
          selector: td.views-field-seeds
        leechers:
          selector: td.views-field-peers
        grabs:
          selector: td.views-field-completed
        downloadvolumefactor:
          text: 0
        uploadvolumefactor:
          text: 1

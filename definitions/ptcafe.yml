# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: ptcafe
  name: PTCafe (咖啡)
  description: PTCafe (咖啡) is a CHINESE Private Torrent Tracker for MOVIES / TV / GENERAL
  language: zh-CN
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/ptcafe.yml
    sourceId: ptcafe
    license: GPL-2.0-only
    fileSha256: 44c3562eda6e57d45e4da39e3b4d140a89f3e0b356fac55b07e12972f8f25566
    contentSha256: 2f41972559f2983de2f2aea662f55ad23316d5d5ca9cc261a1cc169f07abbb59
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://ptcafe.club/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 401
        cat: Movies
        desc: 电影
      - id: 402
        cat: TV
        desc: 剧集
      - id: 403
        cat: TV
        desc: 综艺
      - id: 404
        cat: TV/Documentary
        desc: 纪录
      - id: 405
        cat: TV/Anime
        desc: 动漫
      - id: 406
        cat: Audio/Video
        desc: MV
      - id: 407
        cat: TV/Sport
        desc: 体育
      - id: 408
        cat: Audio
        desc: 音乐
      - id: 409
        cat: Other
        desc: 其他
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        - doubanid
        movie-search:
        - q
        - imdbid
        - doubanid
        music-search:
        - q
    settings:
    - name: username
      type: text
      label: Username
    - name: password
      type: password
      label: Password
    - name: 2facode
      type: text
      label: 2FA code
    - name: info_2fa
      type: info
      label: About 2FA code
      default: Only fill in the <b>2FA code</b> box if you have enabled <b>2FA</b> on the PTCafe Web Site. Otherwise just
        leave it empty.
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: 4
      options:
        4: created
        7: seeders
        5: size
        1: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_tpp
      type: info
      label: Results Per Page
      default: For best results, change the <b>Torrents per page:</b> setting to <b>100</b> on your account profile.
    - name: info_activity
      type: info
      label: Account Inactivity
      default: Retention rules:<ol><li>[Veteran User] and above will be retained forever.</li><li>[Elite User] and above will
        not be deleted after their accounts are blocked.</li><li>Users with a parked account will be banned if they do not
        log in for 365 consecutive days.</li><li>Users with a non-parked account will be banned if they do not log in for
        180 consecutive days.</li><li>Newly registered accounts will be banned by the system if they have no traffic within
        14 days.</li></ol>
    authentication:
      path: login.php
      method: form
      form: form[action="takelogin.php"]
      captcha:
        type: image
        selector: img[alt="CAPTCHA"]
        input: imagestring
      inputs:
        secret: ''
        username: '{{ .Config.username }}'
        password: '{{ .Config.password }}'
        two_step_code: '{{ .Config.2facode }}'
        logout: ''
        securelogin: ''
        ssl: 'yes'
        trackerssl: 'yes'
      error:
      - selector: td.embedded:has(h2:contains("失败"))
        message:
          selector: td.text
      test:
        path: index.php
        selector: a[href="logout.php"]
    search:
      paths:
      - path: torrents.php
      inputs:
        $raw: '{{ range .Categories }}cat{{.}}=1&{{end}}'
        search: '{{ if .Query.IMDBID }}{{ .Query.IMDBID }}{{ else }}{{ end }}{{ if and .Query.DoubanID (eq .Query.IMDBID .False)
          }}{{ .Query.DoubanID }}{{ else }}{{ end }}{{ if and (eq .Query.IMDBID .False) (eq .Query.DoubanID .False) }}{{ .Keywords
          }}{{ else }}{{ end }}'
        incldead: 0
        spstate: '{{ if .Config.freeleech }}2{{ else }}0{{ end }}'
        search_area: '{{ if .Query.IMDBID }}4{{ else }}{{ end }}{{ if and .Query.DoubanID (eq .Query.IMDBID .False) }}1{{
          else }}{{ end }}{{ if and (eq .Query.IMDBID .False) (eq .Query.DoubanID .False) }}0{{ else }}{{ end }}'
        search_mode: 0
        sort: '{{ .Config.sort }}'
        type: '{{ .Config.type }}'
        notnewword: 1
      rows:
        selector: table.torrents > tbody > tr:has(table.torrentname)
      fields:
        category:
          selector: a[href^="?cat="]
          attribute: href
          filters:
          - name: querystring
            args: cat
        title_default:
          selector: a[href^="details.php?id="]
        title:
          selector: a[title][href^="details.php?id="]
          attribute: title
          optional: true
          default: '{{ .Result.title_default }}'
        details:
          selector: a[href^="details.php?id="]
          attribute: href
        download:
          selector: a[href^="download.php?id="]
          attribute: href
        date_elapsed:
          selector: td:nth-child(4) > span[title]
          attribute: title
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-dd HH:mm:ss zzz
        date_added:
          selector: td:nth-child(4):not(:has(span))
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-ddHH:mm:ss zzz
        date:
          text: '{{ if or .Result.date_elapsed .Result.date_added }}{{ or .Result.date_elapsed .Result.date_added }}{{ else
            }}now{{ end }}'
        size:
          selector: td:nth-child(5)
        seeders:
          selector: td:nth-child(6)
        leechers:
          selector: td:nth-child(7)
        grabs:
          selector: td:nth-child(8)
        downloadvolumefactor:
          case:
            img.pro_free: 0
            img.pro_free2up: 0
            img.pro_50pctdown: 0.5
            img.pro_50pctdown2up: 0.5
            img.pro_30pctdown: 0.3
            '*': 1
        uploadvolumefactor:
          case:
            img.pro_50pctdown2up: 2
            img.pro_free2up: 2
            img.pro_2up: 2
            '*': 1
        minimumratio:
          text: 0.8
        description:
          selector: td:nth-child(2)
          remove: a, b, font, img, span

# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: daxiangjiao
  name: DaXiangJiao (大香蕉)
  description: DaXiangJiao (大香蕉) is a CHINESE Private Torrent Tracker for MOVIES / TV / GENERAL
  language: zh-CN
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/daxiangjiao.yml
    sourceId: daxiangjiao
    license: GPL-2.0-only
    fileSha256: 13e50ffb47300f261276699485142070b70402d216e75a45de4ea1b5bc917e89
    contentSha256: ed4502235dd5f2f7c0c703a3ecce3b92c9d1e7e4950d99cabcf535ec3d848587
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://pt.daxiangjiao.org/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 401
        cat: Movies
        desc: Movies/电影
        default: true
      - id: 402
        cat: TV
        desc: TV Series/电视剧
        default: true
      - id: 403
        cat: TV
        desc: TV Shows/综艺
        default: true
      - id: 404
        cat: TV/Documentary
        desc: Documentaries/纪录片
        default: true
      - id: 405
        cat: TV/Other
        desc: Animations/动漫&动画
        default: true
      - id: 406
        cat: Audio/Video
        desc: Music Videos/音乐MV
        default: true
      - id: 407
        cat: TV/Sport
        desc: Sports/赛事
        default: true
      - id: 408
        cat: Audio
        desc: HQ Audio/音轨
        default: true
      - id: 409
        cat: Other/Misc
        desc: Misc/其他
        default: true
      - id: 410
        cat: Movies/Foreign
        desc: Japan and South Korea/日韩
        default: false
      - id: 411
        cat: TV
        desc: Domestic/国产
        default: false
      - id: 412
        cat: TV/Foreign
        desc: Europe and America/欧美
        default: false
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        movie-search:
        - q
        music-search:
        - q
        book-search:
        - q
    settings:
    - name: cookie
      type: text
      label: Cookie
    - name: info_cookie
      type: info_cookie
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: 4
      options:
        4: created
        7: seeders
        5: size
        1: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_tpp
      type: info
      label: Results Per Page
      default: For best results, change the <b>Torrents per page:</b> setting to <b>100</b> on your account profile.
    - name: info_activity
      type: info
      label: Account Inactivity
      default: Cherish your user account. Inactive accounts would be deleted based on the following rules:<ol><li>Veteran
        User or above would never be deleted.</li><li>Elite User or above would never be deleted if parked (at User CP).</li><li>Parked
        accounts would be deleted if users have not logged in for more than 400 days in a row.</li><li>Unparked accounts would
        be deleted if users have not logged in for more than 150 days in a row.</li><li>Accounts with both uploaded and downloaded
        amount being 0 would be deleted if users have not logged in for more than 100 days in a row.</li></ol>
    authentication:
      method: cookie
      inputs:
        cookie: '{{ .Config.cookie }}'
      test:
        path: index.php
        selector: a[href="logout.php"]
    search:
      paths:
      - path: torrents.php
        categories:
        - 401
        - 402
        - 403
        - 404
        - 405
        - 406
        - 407
        - 409
      - path: special.php
        categories:
        - 410
        - 411
        - 412
      inputs:
        $raw: '{{ range .Categories }}cat{{.}}=1&{{end}}'
        search: '{{ .Keywords }}'
        incldead: 0
        spstate: '{{ if .Config.freeleech }}2{{ else }}0{{ end }}'
        search_area: 0
        search_mode: 0
        sort: '{{ .Config.sort }}'
        type: '{{ .Config.type }}'
        notnewword: 1
      rows:
        selector: table.torrents > tbody > tr:has(a[href^="download.php?id="])
      fields:
        category:
          selector: a[href^="?cat="]
          attribute: href
          filters:
          - name: querystring
            args: cat
        title_default:
          selector: a[href^="details.php?id="]
        title:
          selector: a[title][href^="details.php?id="]
          attribute: title
          optional: true
          default: '{{ .Result.title_default }}'
        details:
          selector: a[href^="details.php?id="]
          attribute: href
        download:
          selector: a[href^="download.php?id="]
          attribute: href
        poster:
          selector: img[data-src]
          attribute: data-src
        date_elapsed:
          selector: td:nth-child(4) > span[title]
          attribute: title
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-dd HH:mm:ss zzz
        date_added:
          selector: td:nth-child(4):not(:has(span))
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-ddHH:mm:ss zzz
        date:
          text: '{{ if or .Result.date_elapsed .Result.date_added }}{{ or .Result.date_elapsed .Result.date_added }}{{ else
            }}now{{ end }}'
        size:
          selector: td:nth-child(5)
          optional: true
          default: 512MB
        seeders:
          selector: td:nth-child(6)
          optional: true
          default: 0
        leechers:
          selector: td:nth-child(7)
          optional: true
          default: 0
        grabs:
          selector: td:nth-child(8)
          optional: true
          default: 0
        downloadvolumefactor:
          case:
            img.pro_free: 0
            img.pro_free2up: 0
            img.pro_50pctdown: 0.5
            img.pro_50pctdown2up: 0.5
            img.pro_30pctdown: 0.3
            '*': 1
        uploadvolumefactor:
          case:
            img.pro_50pctdown2up: 2
            img.pro_free2up: 2
            img.pro_2up: 2
            '*': 1
        description:
          selector: td:nth-child(2)
          remove: a, b, font, img, span
        minimumseedtime:
          text: 86400

# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: soulvoice
  name: SoulVoice (聆音Club)
  description: SoulVoice (聆音Club) is a CHINESE Private Torrent Tracker for MOVIES / TV / GENERAL
  language: zh-CN
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/soulvoice.yml
    sourceId: soulvoice
    license: GPL-2.0-only
    fileSha256: 2ae8c1a55fc411da48d4f69d1ed90c28bd6bc8d60b4afc89477b73bfeae1b57a
    contentSha256: 6732dab7bca40029be6e5c504d4ec19b613c94d6c4efcc89a58441d71adf6189
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://pt.soulvoice.club/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 407
        cat: Other
        desc: Study/教学
      - id: 401
        cat: Movies
        desc: Movies/电影
      - id: 402
        cat: TV
        desc: TV Series/电视剧
      - id: 404
        cat: TV/Documentary
        desc: Documentaries/纪录片
      - id: 405
        cat: TV/Anime
        desc: Animations/动漫
      - id: 403
        cat: TV
        desc: TV Show/综艺
      - id: 406
        cat: Audio/Video
        desc: MusicVideo/音乐视频
      - id: 408
        cat: Audio
        desc: HQ Audio/音乐
      - id: 409
        cat: Other
        desc: 其它 (Other)
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        - doubanid
        movie-search:
        - q
        - imdbid
        - doubanid
        music-search:
        - q
    settings:
    - name: username
      type: text
      label: Username
    - name: password
      type: password
      label: Password
    - name: 2facode
      type: text
      label: 2FA code
    - name: info_2fa
      type: info
      label: About 2FA code
      default: Only fill in the <b>2FA code</b> box if you have enabled <b>2FA</b> on the SoulVoice Web Site. Otherwise just
        leave it empty.
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: 4
      options:
        4: created
        7: seeders
        5: size
        1: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_tpp
      type: info
      label: Results Per Page
      default: For best results, change the <b>Torrents per page:</b> setting to <b>100</b> on your account profile.
    - name: info_activity
      type: info
      label: Account Inactivity
      default: Account retention rules:<ol><li>Veteran Users users and above will be retained forever</li><li>Elite Users
        and above will not have their account deleted after parking (in the Control Panel)</li><li>Users with a parked account
        will be deleted if they do not log in for 180 consecutive days</li><li>Users with a non-parked account will be banned
        if they do not log in for 60 consecutive days</li><li>Users with no traffic (ie, upload/download data are both 0)
        will be banned if they do not log in for 7 consecutive days.</li></ol>
    authentication:
      path: login.php
      method: form
      form: form[action="takelogin.php"]
      captcha:
        type: image
        selector: img[alt="CAPTCHA"]
        input: imagestring
      inputs:
        secret: ''
        username: '{{ .Config.username }}'
        password: '{{ .Config.password }}'
        two_step_code: '{{ .Config.2facode }}'
        logout: ''
        securelogin: ''
        ssl: 'yes'
        trackerssl: 'yes'
      error:
      - selector: td.embedded:has(h2:contains("失败"))
      test:
        path: index.php
        selector: a[href="logout.php"]
    search:
      paths:
      - path: torrents.php
      inputs:
        $raw: '{{ range .Categories }}cat{{.}}=1&{{end}}'
        search: '{{ if .Query.IMDBID }}{{ .Query.IMDBID }}{{ else }}{{ end }}{{ if and .Query.DoubanID (eq .Query.IMDBID .False)
          }}{{ .Query.DoubanID }}{{ else }}{{ end }}{{ if and (eq .Query.IMDBID .False) (eq .Query.DoubanID .False) }}{{ .Keywords
          }}{{ else }}{{ end }}'
        incldead: 0
        spstate: '{{ if .Config.freeleech }}2{{ else }}0{{ end }}'
        search_area: '{{ if .Query.IMDBID }}4{{ else }}{{ end }}{{ if and .Query.DoubanID (eq .Query.IMDBID .False) }}1{{
          else }}{{ end }}{{ if and (eq .Query.IMDBID .False) (eq .Query.DoubanID .False) }}0{{ else }}{{ end }}'
        search_mode: 0
        sort: '{{ .Config.sort }}'
        type: '{{ .Config.type }}'
        notnewword: 1
      rows:
        selector: table.torrents > tbody > tr:has(table.torrentname)
      fields:
        category:
          selector: a[href^="?cat="]
          attribute: href
          filters:
          - name: querystring
            args: cat
        title_default:
          selector: a[href^="details.php?id="]
        title:
          selector: a[title][href^="details.php?id="]
          attribute: title
          optional: true
          default: '{{ .Result.title_default }}'
        details:
          selector: a[href^="details.php?id="]
          attribute: href
        download:
          selector: a[href^="download.php?id="]
          attribute: href
        poster:
          selector: img[data-src]
          attribute: data-src
        imdbid:
          selector: a[href*="imdb.com/title/tt"]
          attribute: href
        doubanid:
          selector: a[href*="movie.douban.com/subject/"]
          attribute: href
        date_elapsed:
          selector: td.rowfollow:nth-last-of-type(6) > span[title]
          attribute: title
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-dd HH:mm:ss zzz
        date_added:
          selector: td.rowfollow:nth-last-of-type(6):not(:has(span))
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-ddHH:mm:ss zzz
        date:
          text: '{{ if or .Result.date_elapsed .Result.date_added }}{{ or .Result.date_elapsed .Result.date_added }}{{ else
            }}now{{ end }}'
        size:
          selector: td.rowfollow:nth-last-of-type(5)
        seeders:
          selector: td.rowfollow:nth-last-of-type(4)
        leechers:
          selector: td.rowfollow:nth-last-of-type(3)
        grabs:
          selector: td.rowfollow:nth-last-of-type(2)
        downloadvolumefactor:
          case:
            img.pro_free: 0
            img.pro_free2up: 0
            img.pro_50pctdown: 0.5
            img.pro_50pctdown2up: 0.5
            img.pro_30pctdown: 0.3
            '*': 1
        uploadvolumefactor:
          case:
            img.pro_50pctdown2up: 2
            img.pro_free2up: 2
            img.pro_2up: 2
            '*': 1
        minimumratio:
          text: 2.0
        minimumseedtime:
          text: 259200
        description:
          selector: td.rowfollow:nth-child(2)
          remove: a, b, font, img, span

# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: ptitzmx
  name: PT分享站 (itzmx)
  description: PT分享站 (itzmx) is a CHINESE Private Torrent Tracker for MOVIES / TV / GENERAL
  language: zh-CN
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/ptitzmx.yml
    sourceId: ptitzmx
    license: GPL-2.0-only
    fileSha256: 3fe003de8ef9ff5fc6bfca5f36ecd7f961e77532669e4df14eac57fc912eef7e
    contentSha256: f715290a8d6e6f69639392e34728c57b6c5ec241c2a15735a11c39066d8c0d81
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://pt.itzmx.com/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 405
        cat: TV/Anime
        desc: Anime.动画
      - id: 404
        cat: Books/Comics
        desc: Comics.漫画
      - id: 408
        cat: Audio
        desc: Music.音乐
      - id: 401
        cat: Movies
        desc: Movies.电影
      - id: 402
        cat: TV
        desc: TV.电视剧
      - id: 414
        cat: Movies/BluRay
        desc: MoviesBluray.蓝光
      - id: 410
        cat: PC
        desc: Appz.软件
      - id: 411
        cat: Console
        desc: Games.游戏
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        - doubanid
        movie-search:
        - q
        - imdbid
        - doubanid
        music-search:
        - q
        book-search:
        - q
    settings:
    - name: username
      type: text
      label: Username
    - name: password
      type: password
      label: Password
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: 4
      options:
        4: created
        7: seeders
        5: size
        1: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_tpp
      type: info
      label: Results Per Page
      default: For best results, change the <b>Torrents per page:</b> setting to <b>100</b> on your account profile.
    - name: info_activity
      type: info
      label: Account Inactivity
      default: Cherish your user account. Inactive accounts would be deleted based on the following rules:<ol><li>Veteran
        User and above users will be retained forever</li><li>Elite User and above users will not be deleted after parking
        their accounts (in the control panel)</li><li>Users with parked accounts will have their accounts deleted if they
        do not log in for 300 consecutive days</li><li>Users with un-parked accounts will have their accounts deleted if they
        do not log in for 90 consecutive days</li><li>Accounts with both uploaded and downloaded amount being 0 would be deleted
        if users have not logged in for more than 7 days in a row.</li></ol>
    authentication:
      path: login.php
      method: form
      form: form[action="takelogin.php"]
      captcha:
        type: image
        selector: img[alt="CAPTCHA"]
        input: imagestring
      inputs:
        username: '{{ .Config.username }}'
        password: '{{ .Config.password }}'
        logout: ''
        securelogin: ''
        ssl: 'yes'
        trackerssl: 'yes'
      error:
      - selector: td.embedded:has(h2:contains("失败"))
      test:
        path: index.php
        selector: a[href="logout.php"]
    search:
      paths:
      - path: torrents.php
      inputs:
        $raw: '{{ range .Categories }}cat{{.}}=1&{{end}}'
        search: '{{ if .Query.IMDBID }}{{ .Query.IMDBID }} {{ else }}{{ end }}{{ if .Query.DoubanID }}{{ .Query.DoubanID }}
          {{ else }}{{ end }}{{ .Keywords }}'
        incldead: 0
        spstate: '{{ if .Config.freeleech }}2{{ else }}0{{ end }}'
        search_area: '{{ if or .Query.IMDBID .Query.DoubanID }}1{{ else }}0{{ end }}'
        search_mode: 0
        sort: '{{ .Config.sort }}'
        type: '{{ .Config.type }}'
        notnewword: 1
      rows:
        selector: table.torrents > tbody > tr:has(table.torrentname)
      fields:
        category:
          selector: a[href^="?cat="]
          attribute: href
          filters:
          - name: querystring
            args: cat
        title:
          selector: a[href^="details.php?id="]
          attribute: title
        details:
          selector: a[href^="details.php?id="]
          attribute: href
        download:
          selector: a[href^="download.php?id="]
          attribute: href
        size:
          selector: td:nth-child(5)
        seeders:
          selector: td:nth-child(6)
        leechers:
          selector: td:nth-child(7)
        grabs:
          selector: td:nth-child(8)
        date_elapsed:
          selector: td:nth-child(4) > span[title]
          attribute: title
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-dd HH:mm:ss zzz
        date_added:
          selector: td:nth-child(4):not(:has(span))
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-ddHH:mm:ss zzz
        date:
          text: '{{ if or .Result.date_elapsed .Result.date_added }}{{ or .Result.date_elapsed .Result.date_added }}{{ else
            }}now{{ end }}'
        downloadvolumefactor:
          case:
            img.pro_free: 0
            img.pro_free2up: 0
            img.pro_50pctdown: 0.5
            img.pro_50pctdown2up: 0.5
            img.pro_30pctdown: 0.3
            '*': 1
        uploadvolumefactor:
          case:
            img.pro_50pctdown2up: 2
            img.pro_free2up: 2
            img.pro_2up: 2
            '*': 1
        minimumratio:
          text: 1.0
        description:
          selector: td:nth-child(2)
          remove: a, img

# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: plugintorrent
  name: plugintorrent
  description: plugintorrent is a Public site for AUDIO apps, plugins and samples
  language: en-US
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/plugintorrent.yml
    sourceId: plugintorrent
    license: GPL-2.0-only
    fileSha256: b2f227f83b4e1369998a35b5962662400c7fd6d2a779988c99eb89a889a310a4
    contentSha256: a7c35b1c138d80b8312eef6b95e9abfdaf7e628259b769031478735a27f50be4
spec:
  protocol: torrent
  privacy: public
  baseUrls:
  - https://plugintorrent.com/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categories:
        PC: PC
      modes:
        search:
        - q
    settings:
    - name: sort
      type: select
      label: Sort requested from site
      default: date
      options:
        date: created
        relevance: relevance
    - name: type
      type: select
      label: Order requested from site
      default: DESC
      options:
        DESC: desc
        ASC: asc
    download:
      selectors:
      - selector: a[href^="magnet:?xt"]
        attribute: href
    search:
      paths:
      - path: /
      allowEmptyInputs: true
      inputs:
        s: '{{ .Keywords }}'
        orderby: '{{ .Config.sort }}'
        order: '{{ .Config.type }}'
        post_type: post
      rows:
        selector: article:has(a)
        filters:
        - name: andmatch
      fields:
        category:
          text: PC
        title:
          selector: .entry-title
        details:
          selector: .entry-title > a
          attribute: href
        download:
          selector: .entry-title > a
          attribute: href
        description:
          selector: .entry-summary
        date:
          selector: span.date
        size:
          text: 512 MB
        seeders:
          text: 1
        leechers:
          text: 1
        downloadvolumefactor:
          text: 0
        uploadvolumefactor:
          text: 1

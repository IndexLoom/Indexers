# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: shareisland-api
  name: Shareisland (API)
  description: Shareisland is a ITALIAN Private Torrent Tracker for MOVIES / TV / GENERAL
  language: it-IT
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/shareisland-api.yml
    sourceId: shareisland-api
    license: GPL-2.0-only
    fileSha256: 48128d332f45dd279389e06610b1957a9c22ffecf5b21c3dd904f633cce5716b
    contentSha256: 4938c1166457428460135832d8d6a0c3d89ec52accb0499749d088653d214bbe
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://shareisland.org/
  legacyUrls:
  - http://shareisland.org/
  - http://www.shareisland.org/
  implementation:
    engine: cardigann
    encoding: UTF-8
    capabilities:
      categorymappings:
      - id: 1
        cat: Movies
        desc: Movie
      - id: 28
        cat: Movies
        desc: Foreign Movie
      - id: 2
        cat: TV
        desc: Serie TV
      - id: 15
        cat: Books/EBook
        desc: Ebook
      - id: 26
        cat: Audio/Audiobook
        desc: Audio Book
      - id: 17
        cat: Books/Mags
        desc: Riviste e Giornali
      - id: 3
        cat: Audio
        desc: Music
      - id: 7
        cat: PC/Games
        desc: Games
      - id: 23
        cat: PC
        desc: Software
      - id: 24
        cat: TV/Sport
        desc: Eventi Sportivi
      - id: 25
        cat: Other
        desc: Misc
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        - imdbid
        - tvdbid
        - tmdbid
        movie-search:
        - q
        - imdbid
        - tmdbid
        music-search:
        - q
        book-search:
        - q
    settings:
    - name: apikey
      type: text
      label: APIKey
    - name: info_key
      type: info
      label: About your API key
      default: Find or Generate a new API Token by accessing your <a href="https://shareisland.org/" target="_blank">Shareisland</a>
        account <i>My Settings</i> page and clicking on the <b>API Key</b> tab.
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: single_file_release_use_filename
      type: checkbox
      label: Use filename as title for single file releases
      default: true
    - name: sort
      type: select
      label: Sort requested from site
      default: created_at
      options:
        created_at: created
        seeders: seeders
        size: size
        name: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_activity
      type: info
      label: Account Inactivity
      default: After 60 days of no activity, your account will be automatically deleted.
    authentication:
      path: api/torrents
      method: get
      error:
      - selector: a[href*="/login"]
        message:
          text: The API key was not accepted by {{ .Config.sitelink }}.
      - selector: :root:contains("Account is Banned")
    search:
      paths:
      - path: api/torrents/filter
        response:
          type: json
      headers:
        Authorization:
        - Bearer {{ .Config.apikey }}
      inputs:
        $raw: '{{ range .Categories }}&categories[]={{.}}{{end}}'
        name: '{{ .Keywords }}'
        seasonNumber: '{{ .Query.Season }}'
        episodeNumber: '{{ .Query.Ep }}'
        imdbId: '{{ .Query.IMDBIDShort }}'
        tmdbId: '{{ .Query.TMDBID }}'
        tvdbId: '{{ .Query.TVDBID }}'
        free[]: '{{ if .Config.freeleech }}100{{ else }}{{ end }}'
        sortField: '{{ .Config.sort }}'
        sortDirection: '{{ .Config.type }}'
        perPage: 100
      keywordsfilters:
      - name: re_replace
        args:
        - \b([SE]\d{1,4}){1,2}\b
        - ''
      - name: diacritics
        args: replace
      - name: re_replace
        args:
        - \.
        - ' '
      rows:
        selector: data
        attribute: attributes
      fields:
        category:
          selector: category_id
        title_optional:
          selector: name
        title_filename:
          selector: files[0].name
          optional: true
        files:
          selector: num_file
        title:
          text: '{{ if and (.Config.single_file_release_use_filename) (eq .Result.files "1") (.Result.title_filename) }}{{
            .Result.title_filename }}{{ else }}{{ .Result.title_optional }}{{ end }}'
          filters:
          - name: re_replace
            args:
            - '[\[!"#$%&''()*+,\/:;<=>?@\^`{|}~]'
            - ' '
          - name: diacritics
            args: replace
          - name: re_replace
            args:
            - '[ ]{2,}'
            - ' '
          - name: re_replace
            args:
            - (?i)\bS(\d+)\sE(\d+)\b
            - S$1E$2
          - name: re_replace
            args:
            - (?i)(\d{2})x(\d+)
            - S$1E$2
          - name: re_replace
            args:
            - (?i)\b(\d{1})x(\d+)
            - S0$1E$2
          - name: re_replace
            args:
            - (?i)\bStagion[ei]\s?(\d{1})\b|\bSeason'?s?\s?(\d{1})\b
            - S0$1$2
          - name: re_replace
            args:
            - (?i)\bStagion[ei]\s?(\d{2,})\b|\bSeason'?s?\s?(\d{2,})\b
            - S$1$2
          - name: re_replace
            args:
            - (?i)\b(?:[\/\|]?Episodio\s?(\d+)|Puntata\s?(\d+))
            - E$1$2
          - name: re_replace
            args:
            - (?i)\b(?:Puntate\s*)(\d+)\s?(\d+)
            - E0$1-0$2
          - name: re_replace
            args:
            - (?i)(Serie completa|Completat?a?|in pausa)
            - ''
        details:
          selector: details_link
        download:
          selector: download_link
        poster:
          selector: meta.poster
          filters:
          - name: replace
            args:
            - https://via.placeholder.com/90x135
            - ''
        imdbid:
          selector: imdb_id
        tmdbid:
          selector: tmdb_id
        tvdbid:
          selector: tvdb_id
        genre:
          selector: meta.genres
          filters:
          - name: re_replace
            args:
            - (?i)(televisione film)
            - televisione_film
          - name: replace
            args:
            - ' & '
            - _&_
        _internal:
          selector: internal
          case:
            false: '{{ .False }}'
            true: '{{ .True }}'
        description:
          text: '{{ if .Result._internal }}Internal{{ else }}{{ end }}{{ if and .Result._internal .Result.genre }} | {{ else
            }}{{ end }}{{ .Result.genre }}'
        seeders:
          selector: seeders
        leechers:
          selector: leechers
        grabs:
          selector: times_completed
        date:
          selector: created_at
          filters:
          - name: append
            args: ' +01:00'
          - name: dateparse
            args: MM/dd/yyyy HH:mm:ss zzz
        size:
          selector: size
        _featured:
          selector: featured
          case:
            false: '{{ .False }}'
            true: '{{ .True }}'
        downloadvolumefactor_freeleech:
          selector: freeleech
          case:
            0%: 1
            5%: 0.95
            10%: 0.9
            15%: 0.85
            20%: 0.8
            25%: 0.75
            30%: 0.7
            35%: 0.65
            40%: 0.6
            45%: 0.55
            50%: 0.5
            55%: 0.45
            60%: 0.4
            65%: 0.35
            70%: 0.3
            75%: 0.25
            80%: 0.2
            85%: 0.15
            90%: 0.1
            95%: 0.05
            100%: 0
            '*': 0
        downloadvolumefactor:
          text: '{{ if .Result._featured }}0{{ else }}{{ .Result.downloadvolumefactor_freeleech }}{{ end }}'
        uploadvolumefactor_double_upload:
          selector: double_upload
          case:
            false: 1
            true: 2
        uploadvolumefactor:
          text: '{{ if .Result._featured }}2{{ else }}{{ .Result.uploadvolumefactor_double_upload }}{{ end }}'
        minimumseedtime:
          text: 518400

# SPDX-FileCopyrightText: Jackett and Prowlarr Indexers contributors
# SPDX-License-Identifier: GPL-2.0-only
# Generated by indexloom-importer/v1; do not edit by hand.
apiVersion: indexloom.io/v1alpha1
kind: Indexer
metadata:
  id: haitang
  name: Hǎitáng (海棠PT)
  description: Hǎitáng (海棠PT) is a CHINESE Private Torrent Tracker for OPERA / CROSSTALK
  language: zh-CN
provenance:
  generatedBy: indexloom-importer/v1
  source:
    project: jackett
    repository: https://github.com/Jackett/Jackett
    revision: 79af48a825cfd3a395acc5726b24f6483e83cb52
    path: src/Jackett.Common/Definitions/haitang.yml
    sourceId: haitang
    license: GPL-2.0-only
    fileSha256: 588fa9e89e3693b71b5fe27ea2c7ee749959d7267e63f17419d9fa66372a8ae4
    contentSha256: a3311f58b918ad9181d6668ae1f080bff10d42aafa45a8d8570354a3125b5e74
spec:
  protocol: torrent
  privacy: private
  baseUrls:
  - https://www.htpt.cc/
  implementation:
    engine: cardigann
    encoding: UTF-8
    requestDelaySeconds: 2
    capabilities:
      categorymappings:
      - id: 1
        cat: Audio
        desc: 相声 Crosstalk
        default: true
      - id: 4091
        cat: Audio
        desc: 评书 Storytelling
        default: true
      - id: 4097
        cat: Audio
        desc: 戏曲 Opera
        default: true
      - id: 4098
        cat: Audio
        desc: 鼓/琴 Drum / Piano
        default: true
      - id: 4099
        cat: Audio
        desc: 小曲 Small Song
        default: true
      - id: 4101
        cat: Audio
        desc: 小品 Sketch
        default: true
      - id: 4104
        cat: Audio
        desc: 二人转 Duets
        default: true
      - id: 4105
        cat: TV
        desc: 小剧种 Puppet shows
        default: true
      - id: 4103
        cat: Audio/Audiobook
        desc: 广播剧 Audiobooks
        default: false
      modes:
        search:
        - q
        tv-search:
        - q
        - season
        - ep
        music-search:
        - q
    settings:
    - name: username
      type: text
      label: Username
    - name: password
      type: password
      label: Password
    - name: freeleech
      type: checkbox
      label: Search freeleech only
      default: false
    - name: sort
      type: select
      label: Sort requested from site
      default: 4
      options:
        4: created
        7: seeders
        5: size
        1: title
    - name: type
      type: select
      label: Order requested from site
      default: desc
      options:
        desc: desc
        asc: asc
    - name: info_tpp
      type: info
      label: Results Per Page
      default: For best results, change the <b>Torrents per page:</b> setting to <b>100</b> on your account profile.
    - name: info_activity
      type: info
      label: Account Inactivity
      default: Account retention rules:<ol><li>Users with Veteran User (Wen Chou) and above will be retained forever</li><li>Elite
        User (Wu Sheng) and above will not be deleted after parking (in the control panel)</li><li>Users with a parked account
        who do not log in for 365 consecutive days will be deleted</li><li>Users with a non-parked account who have not logged
        in for 180 consecutive days be deleted</li><li>Users who have no traffic (i.e. upload/download The data is all 0)
        will be deleted If they do not log in for 90 consecutive days.</li></ol>
    authentication:
      path: takelogin.php
      method: post
      inputs:
        username: '{{ .Config.username }}'
        password: '{{ .Config.password }}'
        logout: ''
        securelogin: ''
        ssl: 'yes'
        trackerssl: 'yes'
      error:
      - selector: td.embedded:has(h2:contains("失败"))
      test:
        path: index.php
        selector: a[href="logout.php"]
    search:
      paths:
      - path: torrents.php
        categories:
        - 1
        - 4091
        - 4097
        - 4098
        - 4099
        - 4101
        - 4104
        - 4105
      - path: live.php
        categories:
        - 4103
      inputs:
        $raw: '{{ range .Categories }}cat{{.}}=1&{{end}}'
        search: '{{ .Keywords }}'
        incldead: 0
        spstate: '{{ if .Config.freeleech }}2{{ else }}0{{ end }}'
        search_area: 0
        search_mode: 0
        sort: '{{ .Config.sort }}'
        type: '{{ .Config.type }}'
        notnewword: 1
      rows:
        selector: table.torrents > tbody > tr:has(table.torrentname):not(:has(a[href^="details.php?id="]:contains("请不要下载")))
      fields:
        category:
          selector: a[href^="?cat="]
          attribute: href
          filters:
          - name: querystring
            args: cat
        title_default:
          selector: a[href^="details.php?id="]
        title:
          selector: a[title][href^="details.php?id="]
          attribute: title
          optional: true
          default: '{{ .Result.title_default }}'
        details:
          selector: a[href^="details.php?id="]
          attribute: href
        download:
          selector: a[href^="download.php?id="]
          attribute: href
        date_elapsed:
          selector: td:nth-child(4) > span[title]
          attribute: title
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-dd HH:mm:ss zzz
        date_added:
          selector: td:nth-child(4):not(:has(span))
          optional: true
          filters:
          - name: append
            args: ' +08:00'
          - name: dateparse
            args: yyyy-MM-ddHH:mm:ss zzz
        date:
          text: '{{ if or .Result.date_elapsed .Result.date_added }}{{ or .Result.date_elapsed .Result.date_added }}{{ else
            }}now{{ end }}'
        size:
          selector: td:nth-child(5)
        seeders:
          selector: td:nth-child(6)
        leechers:
          selector: td:nth-child(7)
        grabs:
          selector: td:nth-child(8)
        downloadvolumefactor:
          case:
            img.pro_free: 0
            img.pro_free2up: 0
            img.pro_50pctdown: 0.5
            img.pro_50pctdown2up: 0.5
            img.pro_30pctdown: 0.3
            '*': 1
        uploadvolumefactor:
          case:
            img.pro_50pctdown2up: 2
            img.pro_free2up: 2
            img.pro_2up: 2
            '*': 1
        minimumratio:
          text: 1
        minimumseedtime:
          text: 86400
        description:
          selector: td:nth-child(2)
          remove: a, b, font, img, span
